# Required for automatic @UnderdogNFL recent-search matching.
# Create a read-only bearer token in the X developer portal, then configure it
# as a secret in the hosted site. Never commit the real token.
X_BEARER_TOKEN=
