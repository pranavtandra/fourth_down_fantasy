"use client";

import { useEffect, useMemo, useState } from "react";

type Pos = "QB" | "RB" | "WR" | "TE";
type Player = { name:string; pos:Pos; team:string; adp:number; rank:number; floor:number; ceiling:number; confidence:number; rz:number; role:string; note:string; playerId?:string; depth?:number|null; depthLabel?:string; injuryStatus?:string|null; age?:number|null; live?:boolean; modelContext?:{height:string;weight:string;college:string;teamPassRate:number;positionTargetShare:number;quarterback:string;qbPassTds:number;depthPeers:string[];sample:string} };
type LeagueShape="standard"|"threewr"|"shallow";
type MarketFeed=Record<string,Record<string,number>>;
type LeagueConfig={qb:number;rb:number;wr:number;te:number;flex:number;bench:number;tePremium:number};

const players: Player[] = [
  {name:"Jahmyr Gibbs",pos:"RB",team:"DET",adp:1,rank:1,floor:18.8,ceiling:27.4,confidence:94,rz:96,role:"Three-down centerpiece",note:"Elite receiving floor with expanded goal-line access."},
  {name:"Bijan Robinson",pos:"RB",team:"ATL",adp:2,rank:2,floor:18.4,ceiling:27.8,confidence:94,rz:95,role:"Complete bell cow",note:"The cleanest blend of targets, red-zone work and durability."},
  {name:"Ja'Marr Chase",pos:"WR",team:"CIN",adp:3.2,rank:3,floor:17.9,ceiling:29.1,confidence:93,rz:93,role:"Alpha target dominator",note:"League-winning ceiling; first-read and end-zone volume travel."},
  {name:"Puka Nacua",pos:"WR",team:"LAR",adp:3.8,rank:4,floor:18.1,ceiling:27.0,confidence:92,rz:88,role:"Volume alpha",note:"High-percentage targets create one of the safest WR floors."},
  {name:"Christian McCaffrey",pos:"RB",team:"SF",adp:5.6,rank:5,floor:16.2,ceiling:29.0,confidence:79,rz:97,role:"High-leverage bell cow",note:"Still elite per touch; age and availability widen the range."},
  {name:"Jaxon Smith-Njigba",pos:"WR",team:"SEA",adp:5.5,rank:6,floor:16.7,ceiling:26.1,confidence:88,rz:86,role:"Ascending target hog",note:"Prime-age volume profile with room for another efficiency jump."},
  {name:"Amon-Ra St. Brown",pos:"WR",team:"DET",adp:8.5,rank:7,floor:17.0,ceiling:25.4,confidence:92,rz:89,role:"Slot-plus alpha",note:"Bankable catches and scoring chances in a top offense."},
  {name:"Jonathan Taylor",pos:"RB",team:"IND",adp:7,rank:8,floor:15.6,ceiling:26.8,confidence:86,rz:94,role:"Goal-line engine",note:"Elite carry quality; receiving ceiling trails the top tier."},
  {name:"CeeDee Lamb",pos:"WR",team:"DAL",adp:12.5,rank:9,floor:16.5,ceiling:27.6,confidence:90,rz:90,role:"Featured alpha",note:"Model sees a value if the room lets him escape the first turn."},
  {name:"Justin Jefferson",pos:"WR",team:"MIN",adp:11,rank:10,floor:16.2,ceiling:27.7,confidence:88,rz:87,role:"Boundary alpha",note:"Talent is unquestioned; quarterback environment adds variance."},
  {name:"Ashton Jeanty",pos:"RB",team:"LV",adp:11,rank:11,floor:14.8,ceiling:27.2,confidence:82,rz:92,role:"Volume-driven RB1",note:"Premium workload and valuable touches outweigh modest uncertainty."},
  {name:"Drake London",pos:"WR",team:"ATL",adp:16.5,rank:12,floor:15.2,ceiling:25.8,confidence:87,rz:91,role:"Red-zone alpha",note:"Target depth plus end-zone usage gives him WR1 overall upside."},
  {name:"James Cook",pos:"RB",team:"BUF",adp:11,rank:13,floor:14.7,ceiling:24.7,confidence:86,rz:84,role:"Efficient dual threat",note:"Touchdown regression risk is offset by a strong offense."},
  {name:"Nico Collins",pos:"WR",team:"HOU",adp:15,rank:14,floor:14.9,ceiling:26.0,confidence:84,rz:90,role:"Downfield alpha",note:"High-value targets matter more than raw reception totals."},
  {name:"De'Von Achane",pos:"RB",team:"MIA",adp:13,rank:15,floor:13.8,ceiling:28.4,confidence:78,rz:82,role:"Explosive hybrid",note:"Lower carry floor, but targets and breakaway rate create eruption weeks."},
  {name:"Trey McBride",pos:"TE",team:"ARI",adp:18.5,rank:16,floor:13.8,ceiling:23.9,confidence:91,rz:91,role:"Target-dominant TE",note:"Meaningful positional edge without relying only on touchdowns."},
  {name:"Saquon Barkley",pos:"RB",team:"PHI",adp:14,rank:17,floor:14.1,ceiling:25.0,confidence:80,rz:88,role:"High-volume runner",note:"Strong environment; age and quarterback goal-line carries cap value."},
  {name:"Omarion Hampton",pos:"RB",team:"LAC",adp:16.5,rank:18,floor:13.5,ceiling:25.1,confidence:82,rz:90,role:"Young power feature",note:"Inside-the-10 role can turn good volume into elite volume."},
  {name:"Chase Brown",pos:"RB",team:"CIN",adp:18.5,rank:19,floor:13.7,ceiling:24.2,confidence:83,rz:87,role:"Passing-down feature",note:"Targets lift the floor; watch short-yardage competition."},
  {name:"Brock Bowers",pos:"TE",team:"LV",adp:21,rank:20,floor:13.0,ceiling:24.3,confidence:88,rz:88,role:"Mismatch centerpiece",note:"WR-like route volume offers a real weekly lineup advantage."},
  {name:"A.J. Brown",pos:"WR",team:"NE",adp:17,rank:21,floor:13.8,ceiling:25.5,confidence:79,rz:91,role:"End-zone specialist",note:"Change of environment raises uncertainty but high-value usage remains."},
  {name:"George Pickens",pos:"WR",team:"DAL",adp:16,rank:22,floor:13.1,ceiling:25.8,confidence:76,rz:86,role:"Volatile field stretcher",note:"Ceiling is real; weekly target shape creates a wider floor."},
  {name:"Josh Allen",pos:"QB",team:"BUF",adp:23,rank:23,floor:19.6,ceiling:30.2,confidence:94,rz:98,role:"Dual-threat cheat code",note:"Best QB floor/ceiling blend, but 1QB opportunity cost matters."},
  {name:"Chris Olave",pos:"WR",team:"NO",adp:20,rank:24,floor:13.5,ceiling:23.8,confidence:82,rz:77,role:"Volume separator",note:"Strong target profile; scoring environment is the constraint."},
  {name:"Rashee Rice",pos:"WR",team:"KC",adp:22,rank:25,floor:13.2,ceiling:24.7,confidence:77,rz:85,role:"YAC volume receiver",note:"Efficient role in an elite offense, with availability risk priced in."},
  {name:"Lamar Jackson",pos:"QB",team:"BAL",adp:29,rank:26,floor:19.0,ceiling:30.0,confidence:92,rz:97,role:"Rushing edge QB",note:"Worth the third when elite skill-position tiers are gone."},
  {name:"DeVonta Smith",pos:"WR",team:"PHI",adp:24,rank:27,floor:12.7,ceiling:22.6,confidence:85,rz:79,role:"Efficient co-alpha",note:"Reliable profile, but target competition limits spike-week frequency."},
  {name:"Derrick Henry",pos:"RB",team:"BAL",adp:19,rank:28,floor:12.4,ceiling:25.6,confidence:76,rz:99,role:"Goal-line hammer",note:"Red-zone carries remain elite; receiving and age create fragility."},
  {name:"Brian Thomas Jr.",pos:"WR",team:"JAC",adp:26,rank:29,floor:12.5,ceiling:25.0,confidence:79,rz:84,role:"Vertical alpha",note:"Draft for ceiling, but avoid paying as if target volume is guaranteed."},
  {name:"Malik Nabers",pos:"WR",team:"NYG",adp:25,rank:30,floor:12.9,ceiling:24.8,confidence:78,rz:80,role:"Target monopolist",note:"Volume wins; offensive touchdown expectations restrain the model."},
  {name:"Jayden Daniels",pos:"QB",team:"WAS",adp:35,rank:31,floor:18.0,ceiling:29.4,confidence:85,rz:94,role:"Explosive dual threat",note:"League-winning QB ceiling when the fourth-round skill tier flattens."},
  {name:"Bucky Irving",pos:"RB",team:"TB",adp:31,rank:32,floor:12.5,ceiling:23.4,confidence:81,rz:83,role:"Elusive receiving back",note:"Target floor makes him resilient when game script turns negative."},
  {name:"Tee Higgins",pos:"WR",team:"CIN",adp:34,rank:33,floor:12.4,ceiling:23.5,confidence:80,rz:90,role:"Scoring co-alpha",note:"End-zone role supports a strong ceiling when healthy."},
  {name:"Sam LaPorta",pos:"TE",team:"DET",adp:48,rank:34,floor:10.8,ceiling:21.8,confidence:82,rz:92,role:"Touchdown-rich TE",note:"Strong scoring role, but the model now prices him inside the interchangeable TE tier."},
  {name:"Jalen Hurts",pos:"QB",team:"PHI",adp:39,rank:35,floor:18.1,ceiling:28.8,confidence:88,rz:99,role:"Goal-line QB",note:"Rushing scores carry the profile; passing volume is less certain."},
  {name:"Kenneth Walker",pos:"RB",team:"KC",adp:41,rank:36,floor:11.7,ceiling:23.0,confidence:76,rz:86,role:"Explosive early-down back",note:"New offense creates upside, but role certainty trails his talent."},
  {name:"Colston Loveland",pos:"TE",team:"CHI",adp:52,rank:37,floor:10.5,ceiling:21.6,confidence:75,rz:84,role:"Ascending route winner",note:"Breakout case remains, but a crowded middle tier makes patience more valuable."},
  {name:"Joe Burrow",pos:"QB",team:"CIN",adp:43,rank:38,floor:18.2,ceiling:27.1,confidence:91,rz:86,role:"Elite pocket passer",note:"High floor, but 1QB replacement value argues against reaching."},
  {name:"Tyler Warren",pos:"TE",team:"IND",adp:55,rank:39,floor:10.3,ceiling:21.3,confidence:76,rz:86,role:"Designed-touch TE",note:"Interesting usage, but not enough separation to pay an early-middle-round premium."},
  {name:"Kyren Williams",pos:"RB",team:"LAR",adp:37,rank:40,floor:11.8,ceiling:22.5,confidence:74,rz:93,role:"Scoring-zone back",note:"Goal-line work is excellent, but workload competition adds downside."},
  {name:"Drake Maye",pos:"QB",team:"NE",adp:58,rank:41,floor:17.2,ceiling:28.6,confidence:82,rz:89,role:"Ascending dual threat",note:"Excellent upside if you wait on quarterback."},
  {name:"Tucker Kraft",pos:"TE",team:"GB",adp:69,rank:42,floor:9.8,ceiling:20.4,confidence:73,rz:88,role:"Efficient red-zone TE",note:"Useful later target; touchdown efficiency alone does not justify a scarcity premium."},
  {name:"Caleb Williams",pos:"QB",team:"CHI",adp:72,rank:43,floor:16.8,ceiling:28.1,confidence:78,rz:84,role:"Breakout candidate",note:"Affordable path to top-five QB production."},
  {name:"Kyle Pitts",pos:"TE",team:"ATL",adp:83,rank:44,floor:8.7,ceiling:20.8,confidence:63,rz:81,role:"Volatile seam threat",note:"Draft only after the middle tier falls; volatility is not positional scarcity."},
  {name:"Josh Jacobs",pos:"RB",team:"GB",adp:29.3,rank:45,floor:11.0,ceiling:22.3,confidence:79,rz:92,role:"Veteran scoring back",note:"Touchdown role is strong, but the market price captures much of it."},
  {name:"Jeremiyah Love",pos:"RB",team:"ARI",adp:22.7,rank:46,floor:10.2,ceiling:24.0,confidence:68,rz:86,role:"Rookie upside swing",note:"Explosive profile with a wider role-confidence interval."},
  {name:"Javonte Williams",pos:"RB",team:"DAL",adp:30.7,rank:47,floor:10.6,ceiling:21.7,confidence:74,rz:88,role:"Volume veteran",note:"Useful workload, though efficiency and competition limit the ceiling."},
  {name:"Breece Hall",pos:"RB",team:"NYJ",adp:31.3,rank:48,floor:10.5,ceiling:23.1,confidence:73,rz:79,role:"Receiving-led back",note:"Pass-game work protects the floor when rushing scripts disappoint."},
  {name:"Garrett Wilson",pos:"WR",team:"NYJ",adp:38.7,rank:49,floor:11.1,ceiling:22.8,confidence:77,rz:76,role:"Target-share anchor",note:"Volume remains attractive; scoring environment drives the discount."},
  {name:"Tetairoa McMillan",pos:"WR",team:"CAR",adp:38.7,rank:50,floor:10.7,ceiling:23.3,confidence:72,rz:84,role:"Big-bodied alpha",note:"High-value targets create breakout potential despite volatility."},
  {name:"Cam Skattebo",pos:"RB",team:"NYG",adp:39,rank:51,floor:10.0,ceiling:22.0,confidence:68,rz:90,role:"Contact-volume back",note:"Goal-line access is the draw; health and efficiency widen the range."},
  {name:"Emeka Egbuka",pos:"WR",team:"TB",adp:39,rank:52,floor:10.8,ceiling:22.9,confidence:75,rz:82,role:"Polished target earner",note:"Strong route profile with room for a second-year usage jump."},
  {name:"Zay Flowers",pos:"WR",team:"BAL",adp:39.3,rank:53,floor:11.2,ceiling:21.6,confidence:82,rz:73,role:"Open-field volume WR",note:"Catch volume is stable; modest end-zone work caps the spike case."},
  {name:"Travis Etienne",pos:"RB",team:"NO",adp:42.3,rank:54,floor:10.2,ceiling:21.4,confidence:76,rz:82,role:"Dual-phase starter",note:"Balanced usage gives him multiple paths to weekly points."},
  {name:"Ladd McConkey",pos:"WR",team:"LAC",adp:44,rank:55,floor:11.4,ceiling:22.1,confidence:81,rz:78,role:"Precision volume WR",note:"Efficient target earning makes him safer than the touchdown total suggests."},
  {name:"Quinshon Judkins",pos:"RB",team:"CLE",adp:45.3,rank:56,floor:9.8,ceiling:22.5,confidence:66,rz:87,role:"Power-volume bet",note:"Draft for touch upside but account for offensive uncertainty."},
  {name:"Davante Adams",pos:"WR",team:"LAR",adp:50.3,rank:57,floor:10.6,ceiling:22.0,confidence:76,rz:91,role:"Veteran end-zone alpha",note:"Age lowers the floor, while scoring usage keeps him relevant."},
  {name:"Jaylen Waddle",pos:"WR",team:"DEN",adp:51.3,rank:58,floor:10.4,ceiling:23.0,confidence:72,rz:76,role:"Field-stretching co-alpha",note:"New surroundings add uncertainty and a legitimate ceiling reset."},
  {name:"D.J. Moore",pos:"WR",team:"BUF",adp:51.7,rank:59,floor:10.8,ceiling:22.4,confidence:75,rz:81,role:"YAC-driven starter",note:"Quarterback quality helps, but target competition must be priced."},
  {name:"David Montgomery",pos:"RB",team:"HOU",adp:53,rank:60,floor:9.6,ceiling:20.8,confidence:73,rz:94,role:"Short-yardage specialist",note:"Goal-line carries are the thesis; passing work is secondary."},
  {name:"Luther Burden III",pos:"WR",team:"CHI",adp:53,rank:61,floor:9.5,ceiling:23.2,confidence:67,rz:78,role:"Breakout slot weapon",note:"All-or-nothing builds can lean into his expanding role."},
  {name:"TreVeyon Henderson",pos:"RB",team:"NE",adp:53.7,rank:62,floor:9.7,ceiling:22.8,confidence:70,rz:80,role:"Explosive receiving back",note:"High-value touches make him more interesting than raw carries."},
  {name:"Terry McLaurin",pos:"WR",team:"WAS",adp:54.7,rank:63,floor:10.5,ceiling:21.5,confidence:80,rz:88,role:"Veteran scoring WR",note:"Stable role and quarterback play support a dependable middle-round pick."},
  {name:"D'Andre Swift",pos:"RB",team:"CHI",adp:56.3,rank:64,floor:9.8,ceiling:20.7,confidence:75,rz:75,role:"Receiving-floor back",note:"Targets provide insulation, but inside-the-five work is the concern."},
  {name:"Jameson Williams",pos:"WR",team:"DET",adp:56.7,rank:65,floor:8.9,ceiling:24.8,confidence:68,rz:80,role:"Volatile vertical weapon",note:"An ideal ceiling target for aggressive builds."},
  {name:"Bhayshul Tuten",pos:"RB",team:"JAC",adp:58,rank:66,floor:8.8,ceiling:22.6,confidence:64,rz:83,role:"Young explosive back",note:"Role uncertainty is substantial, but the payoff profile is attractive."},
  {name:"Rome Odunze",pos:"WR",team:"CHI",adp:62.3,rank:67,floor:9.7,ceiling:22.7,confidence:73,rz:85,role:"Ascending boundary WR",note:"End-zone targets give him a clean third-year leap case."},
  {name:"Jadarian Price",pos:"RB",team:"SEA",adp:63.7,rank:68,floor:8.5,ceiling:22.3,confidence:61,rz:84,role:"Rookie workload bet",note:"The price requires comfort with a broad usage range."},
  {name:"Harold Fannin Jr.",pos:"TE",team:"CLE",adp:64.3,rank:69,floor:9.5,ceiling:20.6,confidence:72,rz:85,role:"Move-TE target earner",note:"Volume profile makes him a useful alternative to early tight end."},
  {name:"Carnell Tate",pos:"WR",team:"TEN",adp:66,rank:70,floor:8.6,ceiling:23.0,confidence:62,rz:82,role:"Rookie alpha candidate",note:"Ceiling is stronger than his immediate weekly floor."},
  {name:"Mike Evans",pos:"WR",team:"SF",adp:66.7,rank:71,floor:9.6,ceiling:22.1,confidence:73,rz:95,role:"Veteran touchdown bet",note:"Age risk is real, but end-zone usage remains a bankable skill."},
  {name:"Christian Watson",pos:"WR",team:"GB",adp:75.3,rank:72,floor:8.4,ceiling:22.9,confidence:60,rz:87,role:"High-variance field stretcher",note:"Health and target volume make this a ceiling-only selection."},
  {name:"Jaylen Warren",pos:"RB",team:"PIT",adp:76,rank:73,floor:9.2,ceiling:19.6,confidence:77,rz:69,role:"Passing-down value",note:"Useful floor at a price that does not demand goal-line work."},
  {name:"DK Metcalf",pos:"WR",team:"PIT",adp:77,rank:74,floor:9.0,ceiling:21.8,confidence:72,rz:90,role:"End-zone field stretcher",note:"High-value targets preserve upside in a lower-volume setting."},
  {name:"Tony Pollard",pos:"RB",team:"TEN",adp:77.3,rank:75,floor:9.3,ceiling:19.8,confidence:76,rz:72,role:"Veteran committee lead",note:"Safe touches, but limited high-leverage share restrains the ceiling."},
  {name:"Courtland Sutton",pos:"WR",team:"DEN",adp:78,rank:76,floor:9.8,ceiling:20.5,confidence:79,rz:93,role:"Red-zone veteran",note:"A stable later-round source of targets where they matter most."},
  {name:"Marvin Harrison Jr.",pos:"WR",team:"ARI",adp:78,rank:77,floor:8.8,ceiling:23.1,confidence:66,rz:86,role:"Post-hype alpha",note:"The market discount creates room for patience with development."},
  {name:"Parker Washington",pos:"WR",team:"JAC",adp:81.3,rank:78,floor:8.9,ceiling:20.4,confidence:70,rz:74,role:"Volume slot option",note:"An inexpensive path to weekly catches and flex usability."},
  {name:"Michael Wilson",pos:"WR",team:"ARI",adp:82.3,rank:79,floor:8.5,ceiling:20.8,confidence:69,rz:84,role:"Boundary scoring option",note:"End-zone role makes him a better late target than raw volume suggests."},
  {name:"Jaxson Dart",pos:"QB",team:"NYG",adp:82.7,rank:80,floor:15.0,ceiling:26.1,confidence:68,rz:88,role:"Young rushing QB",note:"Late-QB builds can capture upside without paying an early premium."},
  {name:"Dak Prescott",pos:"QB",team:"DAL",adp:83,rank:81,floor:16.2,ceiling:25.0,confidence:81,rz:75,role:"Volume pocket passer",note:"A dependable wait-on-QB option when healthy."},
  {name:"Rico Dowdle",pos:"RB",team:"PIT",adp:85,rank:82,floor:8.8,ceiling:19.9,confidence:71,rz:80,role:"Affordable volume back",note:"Useful roster depth with contingent upside."},
  {name:"Rhamondre Stevenson",pos:"RB",team:"NE",adp:87,rank:83,floor:8.7,ceiling:20.3,confidence:68,rz:85,role:"Veteran power back",note:"A discounted touchdown path with committee risk."},
  {name:"Chuba Hubbard",pos:"RB",team:"CAR",adp:87,rank:84,floor:9.1,ceiling:19.4,confidence:74,rz:78,role:"Steady workload back",note:"Better for floor-focused builds than ceiling chasers."},
];

const posColor:Record<Pos,string>={QB:"blue",RB:"lime",WR:"violet",TE:"orange"};

type InjuryProfile={risk:number; missed:number; prior:string; trend:string; flag:string};
const injuryProfiles:Record<string,InjuryProfile>={
  "Jahmyr Gibbs":{risk:24,missed:1,prior:"Minor hamstring history",trend:"Stable high usage",flag:"LOW"},
  "Bijan Robinson":{risk:18,missed:0,prior:"No major pro absence",trend:"Gradual workload growth",flag:"LOW"},
  "Ja'Marr Chase":{risk:29,missed:1,prior:"Prior hip / shoulder issues",trend:"Stable target load",flag:"LOW"},
  "Puka Nacua":{risk:58,missed:5,prior:"Recurring knee concerns",trend:"High target concentration",flag:"ELEVATED"},
  "Christian McCaffrey":{risk:82,missed:10,prior:"Calf, Achilles and knee history",trend:"Return from reduced workload",flag:"HIGH"},
  "Jaxon Smith-Njigba":{risk:22,missed:0,prior:"Limited major pro history",trend:"Large but gradual role growth",flag:"LOW"},
  "Amon-Ra St. Brown":{risk:27,missed:1,prior:"Minor ankle / abdominal history",trend:"Stable high route volume",flag:"LOW"},
  "Jonathan Taylor":{risk:62,missed:5,prior:"Recurring ankle and thumb history",trend:"High touch concentration",flag:"ELEVATED"},
  "CeeDee Lamb":{risk:38,missed:2,prior:"Shoulder and lower-body wear",trend:"Sustained elite target volume",flag:"MODERATE"},
  "Justin Jefferson":{risk:47,missed:4,prior:"Prior significant hamstring absence",trend:"Returned to full workload",flag:"MODERATE"},
  "Ashton Jeanty":{risk:32,missed:1,prior:"Short professional history",trend:"Heavy early-career workload",flag:"MODERATE"},
  "Drake London":{risk:31,missed:1,prior:"Minor lower-body history",trend:"Target load trending up",flag:"MODERATE"},
  "De'Von Achane":{risk:55,missed:4,prior:"Knee and soft-tissue history",trend:"Managed carry volume",flag:"ELEVATED"},
  "Trey McBride":{risk:26,missed:1,prior:"Limited major absence",trend:"Heavy route participation",flag:"LOW"},
  "Saquon Barkley":{risk:67,missed:4,prior:"ACL, ankle and accumulated lower-body history",trend:"Very high recent touch load",flag:"ELEVATED"},
  "A.J. Brown":{risk:54,missed:3,prior:"Recurring knee / hamstring concerns",trend:"Team and role transition",flag:"ELEVATED"},
  "Josh Allen":{risk:24,missed:0,prior:"Played through hand / elbow issues",trend:"Stable rushing exposure",flag:"LOW"},
  "Lamar Jackson":{risk:44,missed:2,prior:"Prior ankle / knee absences",trend:"Stable designed-rush volume",flag:"MODERATE"},
  "Derrick Henry":{risk:48,missed:1,prior:"Prior foot injury; age-related exposure",trend:"Sustained high touch load",flag:"MODERATE"},
  "Malik Nabers":{risk:43,missed:2,prior:"Concussion and lower-body history",trend:"Extreme target concentration",flag:"MODERATE"},
  "Jalen Hurts":{risk:39,missed:1,prior:"Knee / shoulder history",trend:"High short-yardage exposure",flag:"MODERATE"},
  "Joe Burrow":{risk:61,missed:3,prior:"Knee, calf and wrist history",trend:"Returned to full dropback load",flag:"ELEVATED"},
  "Kyle Pitts":{risk:52,missed:2,prior:"Prior significant knee injury",trend:"Stable route load",flag:"ELEVATED"},
};
function injuryFor(p:Player):InjuryProfile{return injuryProfiles[p.name]||{risk:Math.max(20,Math.round(62-p.confidence/2)),missed:1,prior:"No major recurring flag in model file",trend:"Role-dependent workload",flag:p.confidence<76?"MODERATE":"LOW"}}
const strategies={balanced:{name:"Balanced",desc:"Best value across positions",tag:"VALUE"},safe:{name:"Safe Floor",desc:"Durability and weekly stability",tag:"FLOOR"},upside:{name:"All or Nothing",desc:"Chase ceiling and breakout paths",tag:"CEILING"},rb:{name:"RB Heavy",desc:"Control scarce early-down volume",tag:"VOLUME"},wr:{name:"WR Heavy",desc:"Stack target earners and flex depth",tag:"TARGETS"},lateqb:{name:"Late QB",desc:"Build skill positions, wait at QB",tag:"PATIENCE"}};

function roundFor(rank:number, teams:number){return Math.max(1,Math.ceil(rank/teams))}
function calibratedProjection(p:Player){const confidence=Math.max(.45,Math.min(.95,p.confidence/100)),mid=p.floor*.62+p.ceiling*.38,prior:Record<Pos,number>={QB:18.2,RB:10.4,WR:10.1,TE:9.8};const projection=mid*confidence+prior[p.pos]*(1-confidence);const uncertainty=(p.ceiling-p.floor)*(1.15-confidence*.45);return{projection,low:Math.max(0,projection-uncertainty*.55),high:projection+uncertainty*.65,confidence}}
function positionalValue(p:Player,superflex:boolean,teams=12,shape:LeagueShape="standard",league?:LeagueConfig){const cfg=league||{qb:1,rb:2,wr:shape==="threewr"?3:2,te:1,flex:1,bench:shape==="shallow"?4:7,tePremium:0},teamShift=(teams-12)*-.16,starterPressure={QB:(cfg.qb+(superflex?cfg.flex*.65:0)-1)*-2.4,RB:(cfg.rb+cfg.flex*.45-2.45)*-1.15,WR:(cfg.wr+cfg.flex*.55-2.55)*-1.05,TE:(cfg.te-1)*-1.5-cfg.tePremium*.7},benchShift=(cfg.bench-7)*-.08;const base:Record<Pos,number>={QB:superflex?14.2:17.2,RB:10.2,WR:9.8,TE:10};const replacement=base[p.pos]+teamShift+starterPressure[p.pos]+benchShift;const demand:Record<Pos,number>={QB:superflex?1.25:.62+Math.max(0,cfg.qb-1)*.45,RB:1.05+cfg.rb*.05+cfg.flex*.03,WR:.88+cfg.wr*.06+cfg.flex*.04,TE:.82+cfg.te*.06+cfg.tePremium*.12};const calibrated=calibratedProjection(p),teBoost=p.pos==="TE"?cfg.tePremium*Math.max(0,p.floor-7)*.35:0,rawEdge=calibrated.projection+teBoost-replacement;return{replacement,projection:calibrated.projection+teBoost,low:calibrated.low,high:calibrated.high+teBoost,edge:rawEdge*demand[p.pos],rawEdge,demand:demand[p.pos]}}
function prospectImpact(p:Player,hasStats:boolean){if(hasStats)return 0;const young=(p.age||25)<=23,depth=p.depth||3,physical=p.modelContext?Number(p.modelContext.weight.replace(/[^0-9]/g,""))||0:0;let impact=young?-4:2;impact+=(depth-1)*4;if(p.pos==="RB"&&physical>=205)impact-=2;if((p.pos==="WR"||p.pos==="TE")&&physical>=210)impact-=1;if(depth>=3)impact+=4;return Math.max(-8,Math.min(16,Math.round(impact)))}
function modelPrimaryRank(p:Player,superflex:boolean,teams=12,shape:LeagueShape="standard",league?:LeagueConfig){const v=positionalValue(p,superflex,teams,shape,league),risk=injuryFor(p).risk,ceilBase:Record<Pos,number>={QB:superflex?20:25,RB:20,WR:20,TE:20},roleWeight=p.pos==="TE"?.08:.15;let strength=v.edge*3+(v.low-v.replacement)*1.05+(v.high-ceilBase[p.pos])*1.2+p.rz*roleWeight+p.confidence*.1-risk*.08;const eliteTe=p.pos==="TE"&&p.floor>=12.5&&p.ceiling>=23&&p.confidence>=85;if(eliteTe)strength+=16;else if(p.pos==="TE")strength-=Math.max(0,12.5-p.floor)*1.4;if(p.pos==="QB"&&!superflex&&p.ceiling>=29)strength+=18;if(p.depth&&p.depth>1)strength-=(p.depth-1)*4;const pure=Math.max(1,Math.min(180,Math.round(155-strength*1.8)));return Math.round(pure*.82+p.adp*.18)}
function adjustedRank(p:Player, scoring:string, superflex:boolean, strategy="balanced",teams=12,shape:LeagueShape="standard",league?:LeagueConfig){let r=modelPrimaryRank(p,superflex,teams,shape,league);const risk=injuryFor(p).risk;const positional=positionalValue(p,superflex,teams,shape,league);if(scoring==="standard"&&p.pos==="RB")r-=2;if(scoring==="standard"&&p.pos==="WR")r+=2;if(scoring==="half"&&p.pos==="RB")r-=1;if(superflex&&p.pos==="QB")r-=Math.max(12,45-r*.35);if(!superflex&&p.pos==="QB")r+=Math.max(5,12-positional.edge*1.6);if(p.pos==="TE"&&!(p.floor>=12.5&&p.ceiling>=23&&p.confidence>=85))r+=8+Math.max(0,12.5-p.floor)*3;if(p.depth&&p.depth>1)r+=(p.depth-1)*(p.pos==="RB"||p.pos==="TE"?5:3);if(p.injuryStatus)r+=/IR|PUP|Out/i.test(p.injuryStatus)?14:/Questionable|Doubtful/i.test(p.injuryStatus)?4:0;if(strategy==="safe"){r+=(risk-35)/6;r-=(p.floor-12)*.55}if(strategy==="upside"){r-=(p.ceiling-23)*.8;r+=(risk-45)/18}if(strategy==="rb"){r+=p.pos==="RB"?-7:p.pos==="WR"?3:1}if(strategy==="wr"){r+=p.pos==="WR"?-7:p.pos==="RB"?4:1}if(strategy==="lateqb"&&!superflex&&p.pos==="QB")r+=18;return Math.max(1,Math.round(r));}
function survivalProbability(p:Player,nextPick:number){const spread=Math.max(5,Math.min(24,5+(100-p.confidence)*.12+(p.ceiling-p.floor)*.55)),z=(nextPick-p.adp)/spread,cdf=1/(1+Math.exp(-1.7*z));return Math.max(1,Math.min(99,Math.round((1-cdf)*100)))}

function upsideWindow(p:Player,scoring:string,superflex:boolean,strategy:string){
  const center=adjustedRank(p,scoring,superflex,strategy),risk=injuryFor(p).risk,spread=p.ceiling-p.floor;
  const positional=positionalValue(p,superflex);const ceilingBase:Record<Pos,number>={QB:superflex?20:25,RB:20,WR:20,TE:20};
  const raw=38+(p.ceiling-ceilingBase[p.pos])*3.3+positional.edge*1.6+spread*1.1+(p.rz-75)*.25+(p.confidence-75)*.1-risk*.16-(p.depth&&p.depth>1?(p.depth-1)*5:0);
  const score=Math.max(20,Math.min(99,Math.round(raw))),width=Math.max(5,Math.round(4+score*.16));
  const early=Math.max(1,center-Math.max(2,Math.round(width*.35))),late=Math.min(180,center+Math.round(width*.65));
  return{score,center,early,late,width:late-early+1,spread,positional};
}

function nameKey(name:string){return name.toLowerCase().replace(/[^a-z0-9 ]/g,"").replace(/\b(jr|sr|ii|iii|iv)\b/g,"").replace(/\s+/g," ").trim()}
function expandedPlayer(raw:any,rank:number):Player{const pos=raw.pos as Pos;const depth=raw.depthOrder||null;const tier=Math.max(0,rank-84);const base=pos==="QB"?15.5:pos==="TE"?8.1:8.6;const ceiling=pos==="QB"?23.8:19.2;return{name:raw.name,pos,team:raw.team,adp:rank,rank,floor:Math.max(5.2,base-tier*.025),ceiling:Math.max(13.5,ceiling-tier*.03),confidence:Math.max(54,70-Math.floor(tier/12)),rz:Math.max(55,78-(depth?Math.max(0,depth-1)*7:5)),role:depth?`${pos}${depth} on current depth chart`:`Current ${pos} depth option`,note:"Extended-board player. Current depth-chart position and availability are included in the opportunity adjustment.",playerId:raw.playerId,depth,depthLabel:raw.depthPosition?String(raw.depthPosition):pos,injuryStatus:raw.injuryStatus,age:raw.age,live:true}}

type AdvancedStat={games:number;carries:number;rushYds:number;rushTd:number;targets:number;receptions:number;recYds:number;recTd:number;airYds:number;yac:number;passAtt:number;passYds:number;passTd:number;interceptions:number;sacks:number;rushFirstDowns:number;recFirstDowns:number;explosiveRuns:number;explosiveCatches:number;ppr:number;targetShare:number;airShare:number;wopr:number;cpoe:number;rushEpa:number;recEpa:number;passEpa:number;epa:number};
function advancedRankImpact(p:Player,s?:AdvancedStat){if(!s||s.games<4)return 0;const clamp=(n:number)=>Math.max(-10,Math.min(10,n));let signal=0;if(p.pos==="RB"){const targets=s.targets/s.games,firstDownRate=s.carries?s.rushFirstDowns/s.carries:0,explosiveRate=s.carries?s.explosiveRuns/s.carries:0,epaPerTouch=(s.rushEpa+s.recEpa)/Math.max(1,s.carries+s.receptions);signal=(targets-2.5)*.9+(firstDownRate-.21)*18+(explosiveRate-.035)*24+epaPerTouch*2.2}else if(p.pos==="WR"||p.pos==="TE"){const firstDownRate=s.targets?s.recFirstDowns/s.targets:0,yacPerCatch=s.yac/Math.max(1,s.receptions),explosiveRate=s.targets?s.explosiveCatches/s.targets:0;const shareBase=.20,airBase=p.pos==="TE"?.18:.20,woprBase=.48;signal=(s.targetShare-shareBase)*24+(s.airShare-airBase)*13+(s.wopr-woprBase)*5+(firstDownRate-.36)*10+(yacPerCatch-4.5)*.18+(explosiveRate-.09)*8}else{const sackRate=s.sacks/Math.max(1,s.passAtt+s.sacks),rushFirstDowns=s.rushFirstDowns/s.games,epaPerGame=s.passEpa/s.games,tdInt=(s.passTd-s.interceptions*.75)/s.games;signal=epaPerGame*.22+(s.cpoe||0)*.12+(rushFirstDowns-1.2)*.7+(tdInt-1.1)*.7-(sackRate-.065)*25}return Math.round(clamp(-signal));}
function supplyRankImpact(p:Player,pool:Player[],teams:number,superflex:boolean){const room=pool.filter(x=>x.pos===p.pos).sort((a,b)=>positionalValue(b,superflex).projection-positionalValue(a,superflex).projection);const index=room.findIndex(x=>nameKey(x.name)===nameKey(p.name));if(index<0)return 0;const posRank=index+1,projection=positionalValue(p,superflex).projection,next=room.slice(index+1,index+4),nextAverage=next.length?next.reduce((sum,x)=>sum+positionalValue(x,superflex).projection,0)/next.length:projection;const nearbyGap=Math.max(0,projection-nextAverage);const premiumCut:Record<Pos,number>={QB:superflex?Math.max(8,teams):5,RB:Math.max(12,Math.round(teams*1.25)),WR:Math.max(18,Math.round(teams*1.75)),TE:2};const replacementCut:Record<Pos,number>={QB:superflex?teams*2:teams,RB:Math.round(teams*2.5),WR:teams*3,TE:teams};const replacement=room[Math.min(room.length-1,Math.max(0,replacementCut[p.pos]-1))];const replacementGap=projection-(replacement?positionalValue(replacement,superflex).projection:projection);const eliteBonus=posRank<=premiumCut[p.pos]?Math.min(8,nearbyGap*4+Math.max(0,replacementGap-2)*.7):0;const flatTier=nearbyGap<1.15;const plateauPenalty=posRank>premiumCut[p.pos]&&flatTier?Math.min(p.pos==="TE"?28:18,(posRank-premiumCut[p.pos])*(p.pos==="TE"?2.4:1.35)+(1.15-nearbyGap)*(p.pos==="TE"?8:5)):0;const replaceablePenalty=posRank<replacementCut[p.pos]&&replacementGap<1?Math.min(5,(1-replacementGap)*5):0;return Math.round(plateauPenalty+replaceablePenalty-eliteBonus)}
function UpsideDraftWindow({player,scoring,superflex,strategy,teams,pick}:{player:Player;scoring:string;superflex:boolean;strategy:string;teams:number;pick:number}){
  const window=upsideWindow(player,scoring,superflex,strategy);const risk=injuryFor(player).risk;
  const call=pick<window.early?"TOO EARLY — WAIT":pick>window.late?"SMASH VALUE":"DRAFTABLE WINDOW";
  const round=(n:number)=>roundFor(n,teams);const earlyRound=round(window.early),lateRound=round(window.late);
  return <section className="upsideWindow"><div className="upsideTop"><div><p className="eyebrow">MODEL UPSIDE × PRICE DISCIPLINE</p><h2>{player.name}&apos;s draftability window</h2><span>The acceptable range expands with modeled upside—not name recognition.</span></div><div className="upsideScore"><b>{window.score}</b><small>UPSIDE / 100</small></div></div><div className="windowCall"><div><span>AT YOUR PICK {pick}</span><strong>{call}</strong></div><div><span>EARLIEST ACCEPTABLE</span><b>Pick {window.early}<small>Round {earlyRound}</small></b></div><div><span>MODEL CENTER</span><b>Pick {window.center}<small>Round {round(window.center)}</small></b></div><div><span>LATEST / SMASH LINE</span><b>Pick {window.late}<small>Round {lateRound}</small></b></div></div><div className="windowTrack"><div className="trackLabels"><span>DO NOT REACH</span><b>{window.width}-PICK DRAFTABLE RANGE</b><span>VALUE ZONE</span></div><div><i style={{left:`${Math.max(0,(window.early-1)/1.8)}%`,width:`${Math.max(3,window.width/1.8)}%`}}/><em style={{left:`${Math.min(99,(window.center-1)/1.8)}%`}}/></div></div><div className="upsideDrivers"><div><span>CEILING</span><b>{player.ceiling.toFixed(1)}</b><small>PPR points / week</small></div><div><span>FLOOR–CEILING SPREAD</span><b>{window.spread.toFixed(1)}</b><small>More breakout room widens the window</small></div><div><span>HIGH-VALUE ROLE</span><b>{player.rz}</b><small>Red-zone + premium-opportunity score</small></div><div><span>AVAILABILITY DRAG</span><b>{risk}</b><small>Higher risk prevents reckless reaches</small></div><div><span>DEPTH POSITION</span><b>{player.depth||"—"}</b><small>Backups lose upside credit</small></div></div><p className="windowMethod"><b>HOW IT WORKS:</b> The model starts at the strategy-adjusted rank, then sizes the acceptable range from absolute weekly ceiling, floor-to-ceiling spread, high-value role, confidence, injury risk and depth-chart order. High-upside players receive wider windows; capped-ceiling players must fall closer to—or beyond—the model price.</p></section>
}
function PositionalValuePanel({player,superflex}:{player:Player;superflex:boolean}){const value=positionalValue(player,superflex);return <section className="positionalPanel"><div><p className="eyebrow">VALUE OVER REPLACEMENT</p><h2>Points are not positional value.</h2><p>{player.pos==="QB"&&!superflex?"In a one-quarterback league, later starters project close enough to most early QBs that raw scoring is discounted. Only a true weekly edge earns an early premium.":player.pos==="QB"?"Superflex changes the equation: playable second quarterbacks are scarce, so the replacement baseline falls and QB demand rises.":`The model compares ${player.name} with a startable ${player.pos} available later, then applies positional lineup demand.`}</p></div><div className="valueMath"><div><span>PLAYER MEDIAN</span><b>{value.projection.toFixed(1)}</b><small>Modeled PPR / week</small></div><i>−</i><div><span>{player.pos} REPLACEMENT</span><b>{value.replacement.toFixed(1)}</b><small>{superflex&&player.pos==="QB"?"Superflex baseline":"Startable-later baseline"}</small></div><i>×</i><div><span>LINEUP DEMAND</span><b>{value.demand.toFixed(2)}</b><small>{player.pos==="QB"&&!superflex?"One starter; deep supply":"Scarcity multiplier"}</small></div><i>=</i><div className="edge"><span>POSITIONAL EDGE</span><b>{value.edge>=0?"+":""}{value.edge.toFixed(1)}</b><small>Adjusted points / week</small></div></div></section>}
function ModelFactorBreakdown({player,superflex,teams,shape,league,advanced}:{player:Player;superflex:boolean;teams:number;shape:LeagueShape;league:LeagueConfig;advanced?:AdvancedStat}){const value=positionalValue(player,superflex,teams,shape,league),injury=injuryFor(player),advancedDelta=advancedRankImpact(player,advanced),prospect=prospectImpact(player,!!advanced),factors=[{label:"ROLE + HIGH-VALUE USE",value:Math.round((player.rz-75)*.32),note:"Red-zone work, targets and valuable touches"},{label:"VALUE OVER REPLACEMENT",value:Math.round(value.edge*2.2),note:`${value.projection.toFixed(1)} projection vs ${value.replacement.toFixed(1)} league baseline`},{label:"AVAILABILITY",value:-Math.round(Math.max(0,injury.risk-28)*.22),note:`${injury.risk}/100 risk index`},{label:"ADVANCED EFFICIENCY",value:-advancedDelta,note:advanced?"Measured prior-season EPA and opportunity":"No qualifying NFL sample"},{label:"ROOKIE / BACKUP PRIOR",value:-prospect,note:advanced?"NFL sample takes precedence":player.depth&&player.depth>1?`Depth-chart position ${player.depth} limits immediate volume`:"Age, physical profile and path to touches"},{label:"UNCERTAINTY SHRINKAGE",value:-Math.round((100-player.confidence)*.12),note:`${player.confidence}% confidence pulls projection toward positional average`}];return <section className="factorBreakdown"><div><p className="eyebrow">MODEL EXPLANATION</p><h2>What moved {player.name}&apos;s projection</h2><p>Every number below is a directional contribution to the model score. Positive factors help; negative factors lower the price.</p></div><div>{factors.map(f=><article key={f.label}><span>{f.label}</span><b className={f.value>=0?"positive":"negative"}>{f.value>=0?"+":""}{f.value}</b><i><em style={{width:`${Math.min(100,Math.abs(f.value)*5)}%`}}/></i><small>{f.note}</small></article>)}</div></section>}
function OnDemandModelContext({player}:{player:Player}){const c=player.modelContext;if(!c)return null;return <section className="demandContext"><div><p className="eyebrow">ON-DEMAND MODEL BUILD</p><h2>Why the model projects {player.name} here</h2><p>{player.note}</p></div><div className="demandFactors"><div><span>PHYSICAL PROFILE</span><b>{c.height} · {c.weight}</b><small>{c.college}</small></div><div><span>TEAM PASS RATE</span><b>{Math.round(c.teamPassRate*100)}%</b><small>2025 verified play mix</small></div><div><span>{player.pos} TARGET POOL</span><b>{Math.round(c.positionTargetShare*100)}%</b><small>Share of team targets to position</small></div><div><span>QUARTERBACK CONTEXT</span><b>{c.quarterback}</b><small>{c.qbPassTds} passing TD in 2025 sample</small></div><div><span>VERIFIED PLAYER SAMPLE</span><b>{c.sample}</b><small>Missing usage is not fabricated</small></div><div><span>DEPTH COMPETITION</span><b>{c.depthPeers.length?c.depthPeers.join(" · "):"No peers returned"}</b><small>Current same-position room</small></div></div><p className="demandCaveat">This is a deterministic model projection, not a sourced scouting claim. Physicals and depth come from Sleeper; team and quarterback tendencies are calculated from the cited nflverse 2025 weekly file. A depth-chart injury changes opportunity, but does not guarantee the replacement player inherits the exact role.</p></section>}
type DepthEntry={name:string;pos:Pos;team:string;depthOrder:number|null;depthPosition:string|null;injuryStatus:string|null};
type InjuryRecord={season:number;week:number;team:string;primary:string;secondary:string|null;gameStatus:string|null;practiceStatus:string|null};
function TeamAndInjuryContext({player}:{player:Player}){
  const [depth,setDepth]=useState<Record<string,DepthEntry[]>>({});const [records,setRecords]=useState<InjuryRecord[]>([]);const [coverage,setCoverage]=useState("2009–2024");const [loading,setLoading]=useState(true);
  useEffect(()=>{let alive=true;setLoading(true);Promise.all([fetch("/api/player-pool").then(r=>r.json()),fetch(`/api/injuries?player=${encodeURIComponent(player.name)}`).then(r=>r.json())]).then(([pool,injuries])=>{if(!alive)return;setDepth(pool.depthCharts?.[player.team]||{});setRecords(injuries.records||[]);setCoverage(injuries.coverage||"2009–2024");setLoading(false)}).catch(()=>setLoading(false));return()=>{alive=false}},[player.name,player.team]);
  const seasons=[...new Set(records.map(r=>r.season))];
  return <section className="contextPanel"><div className="contextHead"><div><p className="eyebrow">TEAM CONTEXT + AVAILABILITY RECORD</p><h2>{player.team} offensive depth chart</h2><span>QB, RB, WR and TE groups · ordered by current listed depth</span></div><span className="verified">● DAILY SYNC</span></div><div className="depthGrid">{(["QB","RB","WR","TE"] as Pos[]).map(pos=><div className="depthGroup" key={pos}><h3><span className={`pos ${posColor[pos]}`}>{pos}</span>{pos} ROOM</h3>{(depth[pos]||[]).slice(0,pos==="WR"?7:5).map((entry,i)=><div className={nameKey(entry.name)===nameKey(player.name)?"focus":""} key={entry.name}><b>{entry.depthOrder||i+1}</b><span>{entry.name}<small>{entry.depthPosition||pos}{entry.injuryStatus?` · ${entry.injuryStatus}`:""}</small></span></div>)}{!depth[pos]?.length&&<p>Depth data unavailable</p>}</div>)}</div><div className="historyHead"><div><p className="eyebrow">WEEK-BY-WEEK INJURY HISTORY</p><h2>{player.name}</h2></div><span>Official-report dataset coverage: {coverage}</span></div>{loading?<div className="historyEmpty">Loading historical reports…</div>:records.length?<div className="historySeasons">{seasons.map(season=><div key={season}><h3>{season} SEASON</h3><div>{records.filter(r=>r.season===season).map((record,i)=><article key={`${record.week}-${record.primary}-${i}`}><b>WK {record.week}</b><div><strong>{record.primary}{record.secondary?` + ${record.secondary}`:""}</strong><span>{record.team}{record.gameStatus?` · Game: ${record.gameStatus}`:""}{record.practiceStatus?` · Practice: ${record.practiceStatus}`:""}</span></div></article>)}</div></div>)}</div>:<div className="historyEmpty"><b>No weekly injury-report record found for {player.name} in {coverage}.</b><p>This means no matching entry was found in this dataset; it does not prove the player never experienced an injury.</p></div>}<div className="historySource"><a href="https://github.com/nflverse/nflverse-data/releases/tag/injuries" target="_blank" rel="noreferrer">Source files · nflverse injuries ↗</a><a href="https://nflreadr.nflverse.com/articles/dictionary_injuries.html" target="_blank" rel="noreferrer">Field definitions ↗</a><p>Rows represent appearances on official weekly practice/game injury reports, not independent medical diagnoses. Repeated daily reports are condensed to one latest record per season, week and injury combination. The public source currently has no 2025 file, so the history clearly stops at 2024.</p></div></section>
}
function AdvancedAnalysis({player}:{player:Player}){
  const [stat,setStat]=useState<AdvancedStat|null>(null);const [loaded,setLoaded]=useState(false);
  useEffect(()=>{let alive=true;setLoaded(false);fetch("/api/advanced").then(r=>r.json()).then(data=>{if(!alive)return;const entry=Object.entries(data.players||{}).find(([name])=>nameKey(name)===nameKey(player.name));setStat((entry?.[1] as AdvancedStat)||null);setLoaded(true)}).catch(()=>setLoaded(true));return()=>{alive=false}},[player.name]);
  if(player.rank>100)return null;
  const pct=(n:number)=>`${Math.round(n*100)}%`;const per=(n:number)=>stat?.games?(n/stat.games).toFixed(1):"—";
  const factors=stat?(player.pos==="RB"?[
    ["RANK IMPACT",`${advancedRankImpact(player,stat)>0?"+":""}${advancedRankImpact(player,stat)}`,"Advanced-stat adjustment"],["TARGETS / GAME",per(stat.targets),"Measured"],["RUSH 1D RATE",stat.carries?pct(stat.rushFirstDowns/stat.carries):"—","Measured"],["EXPLOSIVE RUN RATE",stat.carries?pct(stat.explosiveRuns/stat.carries):"—","Measured"],["EPA / TOUCH",((stat.rushEpa+stat.recEpa)/Math.max(1,stat.carries+stat.receptions)).toFixed(2),"Measured"],["PPR / GAME",per(stat.ppr),"Measured"]
  ]:player.pos==="WR"||player.pos==="TE"?[
    ["RANK IMPACT",`${advancedRankImpact(player,stat)>0?"+":""}${advancedRankImpact(player,stat)}`,"Advanced-stat adjustment"],["TARGET SHARE",pct(stat.targetShare),"Measured weekly average"],["AIR-YARD SHARE",pct(stat.airShare),"Measured weekly average"],["WOPR",stat.wopr.toFixed(2),"Weighted opportunity rating"],["YAC / CATCH",stat.receptions?(stat.yac/stat.receptions).toFixed(1):"—","Measured"],["REC 1D RATE",stat.targets?pct(stat.recFirstDowns/stat.targets):"—","Measured"]
  ]:[
    ["RANK IMPACT",`${advancedRankImpact(player,stat)>0?"+":""}${advancedRankImpact(player,stat)}`,"Advanced-stat adjustment"],["PASS EPA / GAME",stat.games?(stat.passEpa/stat.games).toFixed(2):"—","Measured"],["CPOE",`${stat.cpoe>=0?"+":""}${stat.cpoe.toFixed(1)}`,"Completion % over expected"],["SACK RATE",stat.passAtt?pct(stat.sacks/(stat.passAtt+stat.sacks)):"—","Measured"],["RUSH 1D / GAME",per(stat.rushFirstDowns),"Measured"],["TD–INT",`${stat.passTd}–${stat.interceptions}`,"Measured"]
  ]):[];
  return <section className="advancedPanel"><div className="advancedHead"><div><p className="eyebrow">TOP-100 ADVANCED PROFILE</p><h2>{player.name}: opportunity under the hood</h2><span>{stat?`2025 regular season · ${stat.games} games in dataset · now active in ranking`:loaded?"No verified 2025 sample—rookie or unavailable player. Projection-only factors are intentionally withheld.":"Loading verified season data…"}</span></div><span className={stat?"verified":"pending"}>{stat?"● VERIFIED DATA":"○ CHECKING"}</span></div>{stat&&<><div className="factorGrid">{factors.map(([label,value,kind])=><div key={String(label)}><span>{label}</span><b>{value}</b><small>{kind}</small></div>)}</div><div className="advancedRead"><b>POSITION-SPECIFIC READ</b><p>{player.pos==="RB"?`Targets per game, first-down rate, explosive-run rate and EPA per touch now modify the rank. This distinguishes valuable touches from empty carry volume.`:player.pos==="WR"||player.pos==="TE"?`Target share, air-yard share, WOPR, first-down rate and yards after catch now modify the rank. Volume and downfield opportunity are evaluated separately.`:`Passing EPA, CPOE, sack avoidance, rushing first downs and touchdown-to-interception efficiency now modify the rank. Raw passing yards alone do not drive the result.`}</p></div></>}<div className="sourceLedger"><b>DATA LEDGER</b><a href="https://github.com/nflverse/nflverse-data/releases/tag/stats_player" target="_blank" rel="noreferrer">2025 weekly player statistics · nflverse ↗</a><a href="https://nflreadr.nflverse.com/articles/dictionary_player_stats.html" target="_blank" rel="noreferrer">Field definitions · nflreadr ↗</a><a href="https://operations.nfl.com/gameday/technology/nfl-next-gen-stats" target="_blank" rel="noreferrer">Advanced-stat methodology · NFL ↗</a><a href="https://pubmed.ncbi.nlm.nih.gov/34878369/" target="_blank" rel="noreferrer">Injury recurrence research ↗</a><p>The advanced layer is capped at ±10 ranking slots and requires at least four games, limiting small-sample overreaction. Missing data produces no adjustment. Projections and draft calls remain model outputs—not sourced facts.</p></div></section>;
}

type NewsState={loading:boolean;connected:boolean;posts:{id:string;text:string;created_at?:string;url:string}[];searchUrl:string;message?:string};
type WireItem={title:string;description:string;url:string;published:string;source?:string;reliability?:number;corroboration?:number;label?:string};
type FreeWireState={loading:boolean;connected:boolean;headlines:WireItem[];pastUpdates?:WireItem[];trend:{adds:number;drops:number;net:number;window:string}|null;updatedAt?:string;sources?:{name:string;live:boolean;role?:string;weight?:number}[];coverage?:{live:number;total:number;matched:number;rule:string};archiveCoverage?:{from:string;to:string;windowsLive:number;windowsTotal:number;returned:number;complete:boolean}};
function FreePlayerWire({player}:{player:Player}){const [wire,setWire]=useState<FreeWireState>({loading:true,connected:false,headlines:[],trend:null});useEffect(()=>{let alive=true;const load=()=>fetch(`/api/free-news?player=${encodeURIComponent(player.name)}&id=${encodeURIComponent(player.playerId||"")}`).then(r=>r.json()).then(data=>{if(alive)setWire({...data,loading:false})}).catch(()=>{if(alive)setWire({loading:false,connected:false,headlines:[],trend:null})});load();const timer=setInterval(load,900000);return()=>{alive=false;clearInterval(timer)}},[player.name,player.playerId]);const signal=!wire.trend?"NO SIGNAL":wire.trend.net>0?`+${wire.trend.net} NET ADDS`:wire.trend.net<0?`${wire.trend.net} NET ADDS`:"FLAT";return <section className="freeWire"><div className="freeWireHead"><div><p className="eyebrow">FREE LIVE PLAYER WIRE</p><h2>{player.name} updates</h2><span>Independent feeds · exact-name matching · refreshes every 15 minutes</span></div><span className={wire.connected?"connected":"needsKey"}>{wire.connected?`● ${wire.coverage?.live||0}/${wire.coverage?.total||4} SOURCES LIVE`:"○ FEEDS RETRYING"}</span></div>{wire.sources?.length&&<div className="sourceHealth">{wire.sources.map(source=><span className={source.live?"live":"down"} key={source.name}>{source.live?"●":"○"} {source.name}</span>)}</div>}<div className="trendBand"><div><span>24H SLEEPER SIGNAL</span><strong>{wire.loading?"CHECKING…":signal}</strong></div><div><span>ROSTER ADDS</span><b>{wire.trend?.adds||0}</b></div><div><span>ROSTER DROPS</span><b>{wire.trend?.drops||0}</b></div><p>Add/drop activity is a market signal, not confirmation of news or a model recommendation.</p></div>{wire.loading?<div className="newsEmpty">Checking free sources…</div>:wire.headlines.length?<div className="freeHeadlines">{wire.headlines.map((item,i)=><a href={item.url} target="_blank" rel="noreferrer" key={`${item.url}-${i}`}><span>{item.source||"NEWS"} · {item.published?new Date(item.published).toLocaleString():"Recent"}<em className="reliability">{item.label||"UNRATED"} · {item.reliability||0}/100{item.corroboration&&item.corroboration>1?` · ${item.corroboration} SOURCES`:""}</em></span><strong>{item.title}</strong><p>{item.description}</p><b>READ SOURCE ↗</b></a>)}</div>:<div className="newsEmpty"><b>No recent exact-name headline mentions {player.name}.</b><p>The feeds are working; they return only genuine player matches. Sleeper movement and live injury/depth data remain active even when no article names this player.</p></div>}<div className="freeSources"><a href="https://www.rotowire.com/rss/" target="_blank" rel="noreferrer">RotoWire NFL RSS ↗</a><a href="https://www.espn.com/espn/rss/nfl/news" target="_blank" rel="noreferrer">ESPN NFL RSS ↗</a><a href="https://docs.sleeper.com/" target="_blank" rel="noreferrer">Sleeper API ↗</a><span>No paid key · independent fallbacks · exact-name matching</span></div></section>}

function PastPlayerUpdates({player}:{player:Player}){const [archive,setArchive]=useState<{items:WireItem[];loaded:boolean;coverage?:FreeWireState["archiveCoverage"]}>({items:[],loaded:false});useEffect(()=>{let alive=true;setArchive({items:[],loaded:false});fetch(`/api/free-news?player=${encodeURIComponent(player.name)}&id=${encodeURIComponent(player.playerId||"")}`).then(r=>r.json()).then(data=>{if(alive)setArchive({items:data.pastUpdates||[],loaded:true,coverage:data.archiveCoverage})}).catch(()=>{if(alive)setArchive({items:[],loaded:true})});return()=>{alive=false}},[player.name,player.playerId]);const {items,loaded,coverage}=archive;return <section className="pastUpdates"><div className="pastUpdatesHead"><div><p className="eyebrow">ROLLING 12-MONTH PLAYER ARCHIVE</p><h2>Past {player.name} updates</h2><span>{coverage?`${coverage.from} through ${coverage.to} · ${coverage.windowsLive}/${coverage.windowsTotal} archive windows loaded`:"Five date-bounded searches prevent recent news from crowding out older reports."}</span></div><b>{items.length} OLDER UPDATES</b></div>{coverage&&<div className={`archiveStatus ${coverage.complete?"complete":"partial"}`}><b>{coverage.complete?"● FULL YEAR SEARCHED":"◐ PARTIAL ARCHIVE"}</b><span>{coverage.returned} total exact-name matches, including the six newest updates shown above.</span></div>}{!loaded?<div className="historyEmpty">Searching the full prior year…</div>:items.length?<div className="pastUpdateList">{items.map((item,i)=><a href={item.url} target="_blank" rel="noreferrer" key={`${item.url}-${i}`}><time>{item.published?new Date(item.published).toLocaleDateString():"Date unavailable"}</time><div><span>{item.source||"NEWS"} · {item.label||"SINGLE-SOURCE"} {item.reliability||0}/100</span><strong>{item.title}</strong><p>{item.description}</p></div><b>OPEN ↗</b></a>)}</div>:<div className="historyEmpty"><b>No older exact-name coverage returned.</b><p>All five archive windows were searched when marked complete. The archive does not invent or substitute unrelated team stories.</p></div>}<p className="pastUpdatesNote">Coverage is a rolling 365 days and is split into five date windows. Results combine exact-name Google News archive matches with ESPN, RotoWire and Bing; duplicates are collapsed. “Full year searched” describes query coverage, not a guarantee that every publisher is indexed.</p></section>}

type RankedPlayer=Player&{custom:number;modelPrice?:number};
type ValidationMetric={sample:number;modelSpearman:number;baselineSpearman:number;modelMae:number;baselineMae:number};
type BacktestResult=ValidationMetric&{season:number;positions:Record<Pos,ValidationMetric>;teCalibration:{actualTop50:number;modelTop50:number;baselineTop50:number}};
function PositionValidation({test}:{test:{results:BacktestResult[];positionSummary?:Record<Pos,ValidationMetric>;target?:string}}){const summary=test.positionSummary;return <section className="positionValidation"><div className="positionValidationHead"><div><p className="eyebrow">POSITIONAL CALIBRATION</p><h2>Does the model price positions correctly?</h2><p>{test.target}</p></div><span>ρ = RANK CORRELATION</span></div>{summary&&<div className="positionValidationGrid">{(["QB","RB","WR","TE"] as Pos[]).map(pos=><article key={pos}><span className={`pos ${posColor[pos]}`}>{pos}</span><b>{summary[pos].modelSpearman.toFixed(2)} ρ</b><small>{summary[pos].modelMae.toFixed(1)}-pick mean error · {summary[pos].sample} samples</small><em>{summary[pos].modelSpearman>=summary[pos].baselineSpearman?"BEATS PRIOR-FINISH BASELINE":"NEEDS RECALIBRATION"}</em></article>)}</div>}<div className="teCalibration"><div><p className="eyebrow">TIGHT END TOP-50 CHECK</p><h3>Outliers only—not the whole middle tier.</h3><p>The model is compared with how many tight ends actually finished among the top 50 league-adjusted values in each test season.</p></div>{test.results.map(r=><article key={r.season}><b>{r.season}</b><span>ACTUAL <strong>{r.teCalibration.actualTop50}</strong></span><span>MODEL <strong>{r.teCalibration.modelTop50}</strong></span><small className={Math.abs(r.teCalibration.modelTop50-r.teCalibration.actualTop50)<=1?"pass":"warn"}>{Math.abs(r.teCalibration.modelTop50-r.teCalibration.actualTop50)<=1?"CALIBRATED":"REVIEW"}</small></article>)}</div></section>}
function ValidationPage({ranked}:{ranked:RankedPlayer[]}){
  const [test,setTest]=useState<{loading:boolean;connected:boolean;results:BacktestResult[];summary?:{seasons:number;sample:number;modelSpearman:number;baselineSpearman:number;modelMae:number;baselineMae:number};positionSummary?:Record<Pos,ValidationMetric>;coverage?:string;method?:string;target?:string;message?:string}>({loading:true,connected:false,results:[]});
  useEffect(()=>{let alive=true;fetch("/api/backtest").then(r=>r.json()).then(data=>{if(alive)setTest({...data,loading:false})}).catch(()=>{if(alive)setTest({loading:false,connected:false,results:[],message:"Historical feeds are temporarily unavailable."})});return()=>{alive=false}},[]);
  const comparable=ranked.filter(p=>p.adp>0&&p.adp<=180),meanGap=comparable.reduce((s,p)=>s+Math.abs(p.custom-p.adp),0)/Math.max(1,comparable.length),agreement=comparable.filter(p=>Math.abs(p.custom-p.adp)<=6).length/Math.max(1,comparable.length),byPos=(["QB","RB","WR","TE"] as Pos[]).map(pos=>{const rows=comparable.filter(p=>p.pos===pos);return{pos,count:rows.length,gap:rows.reduce((s,p)=>s+Math.abs(p.custom-p.adp),0)/Math.max(1,rows.length),early:rows.filter(p=>p.custom+6<p.adp).length,late:rows.filter(p=>p.custom>p.adp+6).length}}),disagreements=comparable.slice().sort((a,b)=>Math.abs(b.adp-b.custom)-Math.abs(a.adp-a.custom)).slice(0,12);
  return <section className="validationPage"><div className="validationHero"><p className="eyebrow">REPRODUCIBLE MODEL CHECKS</p><h1>Trust the test,<br/><em>not the pitch.</em></h1><p>Historical accuracy and current market disagreement are different questions. This page shows both, labels the comparison baseline, and hides accuracy numbers when the source data cannot be reproduced.</p></div><div className="validationStats"><article><span>HISTORICAL TEST</span><b>{test.loading?"…":test.connected?`${test.summary?.seasons} SEASONS`:"OFFLINE"}</b><small>{test.connected?`${test.summary?.sample} matched player-seasons`:`No cached claims substituted`}</small></article><article><span>RANK CORRELATION</span><b>{test.connected?test.summary?.modelSpearman.toFixed(2):"—"}</b><small>Spearman; 1.00 is perfect ordering</small></article><article><span>CURRENT MARKET GAP</span><b>{meanGap.toFixed(1)}</b><small>Mean absolute picks vs ESPN snapshot</small></article><article><span>WITHIN 6 PICKS</span><b>{Math.round(agreement*100)}%</b><small>Model–market agreement rate</small></article></div>{test.connected?<><div className="backtestHead"><div><p className="eyebrow">WALK-FORWARD BACKTEST · {test.coverage}</p><h2>Model signal vs next-season positional value</h2><p>{test.method} {test.target}</p></div><span>● NFLVERSE VERIFIED</span></div><div className="backtestTable"><table><thead><tr><th>SEASON</th><th>MATCHED</th><th>MODEL ρ</th><th>PRIOR-FINISH ρ</th><th>MODEL RANK ERROR</th><th>BASELINE ERROR</th></tr></thead><tbody>{test.results.map(r=><tr key={r.season}><td><b>{r.season}</b></td><td>{r.sample}</td><td><b>{r.modelSpearman.toFixed(2)}</b></td><td>{r.baselineSpearman.toFixed(2)}</td><td>{r.modelMae.toFixed(1)} picks</td><td>{r.baselineMae.toFixed(1)} picks</td></tr>)}</tbody></table></div></>:<div className="backtestUnavailable"><b>{test.loading?"RUNNING HISTORICAL TEST…":"BACKTEST DATA UNAVAILABLE"}</b><p>{test.message||"The app will retry the free historical feeds. Accuracy numbers are intentionally withheld until the calculation completes."}</p></div>}{test.connected&&<PositionValidation test={test}/>}<div className="marketHead"><div><p className="eyebrow">CURRENT BOARD AUDIT</p><h2>Model rank vs ESPN ADP</h2><p>ADP is a market-price comparison, not ground truth. Large gaps flag where assumptions deserve review.</p></div><b>{comparable.length} PLAYERS COMPARED</b></div><div className="positionAudit">{byPos.map(x=><article key={x.pos}><span className={`pos ${posColor[x.pos]}`}>{x.pos}</span><b>{x.gap.toFixed(1)} picks</b><small>mean gap · {x.early} model earlier · {x.late} model later</small></article>)}</div><div className="disagreementGrid">{disagreements.map(p=><article key={p.name}><span className={`pos ${posColor[p.pos]}`}>{p.pos}</span><div><b>{p.name}</b><small>{p.team} · {p.role}</small></div><em className={p.custom<p.adp?"modelEarly":"modelLate"}>{p.custom<p.adp?"MODEL EARLIER":"MODEL LATER"} {Math.round(Math.abs(p.adp-p.custom))}</em><dl><div><dt>MODEL</dt><dd>#{p.custom}</dd></div><div><dt>ESPN ADP</dt><dd>{p.adp}</dd></div></dl></article>)}</div><p className="validationNote"><b>LIMITATION:</b> The historical test validates a frozen, preseason-safe signal built from prior-season production, usage and high-value opportunity. It does not retroactively test 2026 depth charts, injuries or coaching assumptions. Players without a prior six-game sample are excluded, so rookies require separate evaluation.</p></section>
}
function TeamAnalyzer({ranked}:{ranked:RankedPlayer[]}){
  const [roster,setRoster]=useState("Jahmyr Gibbs\nJa'Marr Chase\nJosh Allen\nTrey McBride\nLadd McConkey\nBucky Irving");const [leagueTeams,setLeagueTeams]=useState(12);const [format,setFormat]=useState("ppr");const [qb,setQb]=useState(1);const [rb,setRb]=useState(2);const [wr,setWr]=useState(2);const [te,setTe]=useState(1);const [flex,setFlex]=useState(2);const [bench,setBench]=useState(6);const [sf,setSf]=useState(false);const [tePremium,setTePremium]=useState(false);const [passTd,setPassTd]=useState(4);const [riskMode,setRiskMode]=useState("balanced");
  const names=roster.split(/\n|,/).map(x=>x.replace(/^\s*\d+[.)-]?\s*/,"").trim()).filter(Boolean);const matched=names.map(name=>ranked.find(p=>nameKey(p.name)===nameKey(name))).filter(Boolean) as RankedPlayer[];const unknown=names.length-matched.length;const counts={QB:matched.filter(p=>p.pos==="QB").length,RB:matched.filter(p=>p.pos==="RB").length,WR:matched.filter(p=>p.pos==="WR").length,TE:matched.filter(p=>p.pos==="TE").length};const requirements={QB:qb+(sf?1:0),RB:rb,WR:wr,TE:te};const holes=(Object.keys(requirements) as Pos[]).filter(pos=>counts[pos]<requirements[pos]);const avgRank=matched.length?matched.reduce((s,p)=>s+p.custom,0)/matched.length:180;const avgUpside=matched.length?matched.reduce((s,p)=>s+upsideWindow(p,format,sf,riskMode).score,0)/matched.length:0;const avgRisk=matched.length?matched.reduce((s,p)=>s+injuryFor(p).risk,0)/matched.length:100;const value=matched.reduce((s,p)=>s+(p.adp-p.custom),0);const completion=Math.min(1,matched.length/Math.max(1,qb+rb+wr+te+flex));const formatBoost=(tePremium?counts.TE*2:0)+(passTd===6?counts.QB*1.5:0);const grade=matched.length?Math.max(35,Math.min(99,Math.round(58+completion*18+value/matched.length+avgUpside*.12-avgRisk*.08+formatBoost-holes.length*5))):0;const used=new Set(matched.map(p=>p.name));
  const rosterCapacity=qb+rb+wr+te+flex+bench;const remainingSpots=Math.max(0,rosterCapacity-matched.length);const flexEligibleCount=counts.RB+counts.WR+counts.TE;const flexBase=rb+wr+te;const openFlex=Math.max(0,flex-(Math.max(0,flexEligibleCount-flexBase)));const benchBudget=Math.max(0,remainingSpots-holes.length-openFlex);
  const desiredDepth:Record<Pos,number>={QB:requirements.QB+(sf&&bench>=5?1:0),RB:requirements.RB+Math.ceil(flex*.45)+Math.ceil(benchBudget*.45),WR:requirements.WR+Math.ceil(flex*.5)+Math.ceil(benchBudget*.45),TE:requirements.TE+(tePremium&&bench>=4?1:0)};
  const positionRisk=(position:Pos)=>{const room=matched.filter(p=>p.pos===position);return room.length?room.reduce((sum,p)=>sum+injuryFor(p).risk,0)/room.length:100};
  const positionalRank=(player:RankedPlayer)=>ranked.filter(p=>p.pos===player.pos&&p.custom<=player.custom).length;const premiumStarter=(position:"QB"|"TE")=>matched.filter(p=>p.pos===position).some(p=>positionalRank(p)<=4&&injuryFor(p).risk<55);const solvedPremium=(position:Pos)=>(position==="QB"&&!sf&&counts.QB>=qb&&premiumStarter("QB"))||(position==="TE"&&!tePremium&&counts.TE>=te&&premiumStarter("TE"));
  const recommendations=ranked.filter(p=>!used.has(p.name)&&!solvedPremium(p.pos)).map(p=>{const window=upsideWindow(p,format,sf,riskMode);const shortfall=Math.max(0,desiredDepth[p.pos]-counts[p.pos]);const starterNeed=counts[p.pos]<requirements[p.pos];const flexFit=(p.pos==="RB"||p.pos==="WR"||(p.pos==="TE"&&tePremium))&&openFlex>0;const roomRisk=positionRisk(p.pos);const teammate=matched.find(existing=>existing.pos==="RB"&&p.pos==="RB"&&existing.team===p.team&&injuryFor(existing).risk>=50);const replacementRank=ranked.findIndex(x=>x.pos===p.pos&&x.custom>=leagueTeams*Math.max(1,requirements[p.pos]))+1;const scarcity=replacementRank>0?Math.max(0,28-replacementRank*.18):0;let fit=145-p.custom*.58+shortfall*17+(starterNeed?105:0)+(flexFit?openFlex*20:0)+scarcity;if(p.pos==="QB")fit+=sf?32:counts.QB>=requirements.QB?-48:passTd===6?8:0;if(p.pos==="TE")fit+=tePremium?24:counts.TE>=requirements.TE?-30:0;if((p.pos==="RB"||p.pos==="WR")&&counts[p.pos]===0)fit+=14;if(teammate)fit+=16;if(remainingSpots<=Math.max(1,holes.length)&&!starterNeed)fit-=70;if(riskMode==="safe")fit+=p.floor*1.8-roomRisk*.08;else if(riskMode==="upside")fit+=p.ceiling*1.4+window.score*.2;else fit+=p.floor*.7+p.ceiling*.7;if(avgUpside<65)fit+=window.score*.18;if(roomRisk>=50)fit+=p.floor*.75;let tag="MODEL VALUE";let reason=`Highest marginal value for this lineup after positional replacement cost.`;if(starterNeed){tag="STARTER NEED";reason=`Fills an open ${p.pos} starter; lineup coverage outweighs luxury depth.`}else if(flexFit&&shortfall>0){tag="FLEX + DEPTH";reason=`Competes for ${openFlex} open flex slot${openFlex===1?"":"s"} while building usable ${p.pos} depth.`}else if(teammate){tag="CONTINGENCY";reason=`Adds direct backfield coverage for ${teammate.name}'s elevated availability risk.`}else if(shortfall>0){tag="DEPTH NEED";reason=`Your ${p.pos} room has ${counts[p.pos]} players versus a situation-based target of ${desiredDepth[p.pos]}.`}else if(avgUpside<65&&window.score>=65){tag="UPSIDE";reason="Raises the ceiling of a roster currently below the model's upside target."}else if(roomRisk>=50&&p.floor>=12){tag="FLOOR HEDGE";reason=`Supplies stable weekly volume behind a higher-risk ${p.pos} room.`}return {...p,fit,tag,reason};}).sort((a,b)=>b.fit-a.fit||a.custom-b.custom);
  const weakestStarter=(position:Pos)=>matched.filter(p=>p.pos===position).sort((a,b)=>b.custom-a.custom)[0];const roomStrength=(position:Pos)=>{const room=matched.filter(p=>p.pos===position).sort((a,b)=>a.custom-b.custom).slice(0,Math.max(1,requirements[position]));return room.length?room.reduce((sum,p)=>sum+p.custom,0)/room.length:180};const weakPositions=(["QB","RB","WR","TE"] as Pos[]).filter(p=>!solvedPremium(p)).sort((a,b)=>(holes.includes(b)?100:0)+roomStrength(b)-(holes.includes(a)?100:0)-roomStrength(a));const strongRoster=grade>=78&&!holes.length;
  const tradeTargets=recommendations.filter(p=>weakPositions.slice(0,2).includes(p.pos)&&p.custom>=Math.max(14,Math.round(grade*.22))&&p.custom<=Math.min(90,Math.round(112-grade*.35))&&(!weakestStarter(p.pos)||p.custom<weakestStarter(p.pos)!.custom-4)).slice(0,strongRoster?2:4).map(p=>({...p,acquisition:"TRADE TARGET",tag:"REALISTIC UPGRADE",reason:`Attainable ${p.pos} upgrade for a room with a ${Math.round(roomStrength(p.pos))} average starter rank; the model prefers the role without pricing in an elite-tier trade cost.`}));
  const buyLowTargets=recommendations.filter(p=>p.custom>=Math.max(48,leagueTeams*4)&&p.adp-p.custom>=4&&!tradeTargets.some(t=>t.name===p.name)).sort((a,b)=>(b.adp-b.custom)-(a.adp-a.custom)||b.fit-a.fit).slice(0,strongRoster?4:2).map(p=>({...p,acquisition:"BUY-LOW",tag:`MODEL +${Math.round(p.adp-p.custom)}`,reason:`The model ranks ${p.name} ${Math.round(p.adp-p.custom)} spots ahead of ADP. That market discount plus ${p.role.toLowerCase()} creates a low-cost depth opportunity.`}));
  const nextContext=[...tradeTargets,...buyLowTargets].sort((a,b)=>strongRoster?(a.acquisition==="BUY-LOW"?-1:1):(a.acquisition==="TRADE TARGET"?-1:1)).slice(0,6);
  return <section className="teamAnalyzer"><div className="teamIntro"><p className="eyebrow">MODEL-BASED ROSTER DIAGNOSTICS</p><h1>Analyze the team,<br/><em>not just the picks.</em></h1><p>Paste a roster, mirror the league format, and the model will grade positional value, starter coverage, upside, availability risk and replacement-level advantage.</p></div><div className="leagueBuilder"><h2>League settings</h2><div className="settingGrid"><label>TEAMS<select value={leagueTeams} onChange={e=>setLeagueTeams(+e.target.value)}>{[8,10,12,14,16].map(x=><option key={x}>{x}</option>)}</select></label><label>SCORING<select value={format} onChange={e=>setFormat(e.target.value)}><option value="ppr">Full PPR</option><option value="half">Half PPR</option><option value="standard">Standard</option></select></label><label>PASS TD<select value={passTd} onChange={e=>setPassTd(+e.target.value)}><option value="4">4 points</option><option value="6">6 points</option></select></label><label>RISK PROFILE<select value={riskMode} onChange={e=>setRiskMode(e.target.value)}><option value="balanced">Balanced</option><option value="safe">Safe floor</option><option value="upside">All or nothing</option></select></label></div><div className="slotGrid">{[["QB",qb,setQb],["RB",rb,setRb],["WR",wr,setWr],["TE",te,setTe],["FLEX",flex,setFlex],["BENCH",bench,setBench]].map(([label,val,setter])=><label key={String(label)}>{label}<input type="number" min="0" max="12" value={val as number} onChange={e=>(setter as (n:number)=>void)(+e.target.value)}/></label>)}</div><div className="advancedToggles"><button className={sf?"on":""} onClick={()=>setSf(!sf)}><b>SUPERFLEX</b><span>{sf?"ON":"OFF"}</span></button><button className={tePremium?"on":""} onClick={()=>setTePremium(!tePremium)}><b>TE PREMIUM</b><span>{tePremium?"ON":"OFF"}</span></button></div><small>{leagueTeams}-team · {qb+(sf?1:0)+rb+wr+te+flex} starters · {bench} bench spots</small></div><div className="rosterEntry"><h2>Your roster</h2><p>One player per line. Numbering is optional.</p><textarea aria-label="Team roster" value={roster} onChange={e=>setRoster(e.target.value)}/><span>{matched.length} recognized · {unknown} unmatched</span></div><div className="teamGrade"><div className="gradeHero"><span>MODEL TEAM GRADE</span><strong>{grade||"—"}</strong><small>/ 100</small><p>{holes.length?`Primary need: ${holes.join(", ")}`:"Starting structure is covered"}</p></div><div className="teamMetrics"><div><span>AVG MODEL RANK</span><b>{matched.length?avgRank.toFixed(1):"—"}</b></div><div><span>UPSIDE</span><b>{matched.length?Math.round(avgUpside):"—"}</b></div><div><span>INJURY RISK</span><b>{matched.length?Math.round(avgRisk):"—"}</b></div><div><span>VALUE VS ADP</span><b>{value>=0?`+${Math.round(value)}`:Math.round(value)}</b></div></div><div className="positionHealth">{(["QB","RB","WR","TE"] as Pos[]).map(pos=><div key={pos}><span className={`pos ${posColor[pos]}`}>{pos}</span><b>{counts[pos]} / {requirements[pos]}</b><small>{solvedPremium(pos)?"ELITE / SOLVED":counts[pos]>=requirements[pos]?"COVERED":"NEEDS HELP"}</small></div>)}</div><div className="teamRead"><b>MODEL READ</b><p>{holes.length?`The roster is structurally thin at ${holes.join(" and ")}. The next recommendation prioritizes starter-level replacement value there before adding luxury depth.`:`The starters are covered. ${avgUpside>=65?"This build carries strong ceiling; protect it with stable bench volume.":"The roster is stable but could use another high-ceiling outcome."}`} {solvedPremium("QB")||solvedPremium("TE")?"Your premium QB/TE investment is treated as a solved lineup slot, preserving bench space for RB and WR outcomes.":""} {avgRisk>50?"Availability risk is concentrated enough to justify safer bench coverage.":"The availability profile is manageable."}</p></div></div><div className="nextTargets contextualTargets"><div className="targetHeading"><p className="eyebrow">REALISTIC NEXT MOVES</p><h2>{strongRoster?"Buy-low depth first":"Trade help + buy lows"}</h2><small>{strongRoster?"A strong roster should preserve assets and hunt model-backed market discounts.":`The model targets attainable upgrades at ${weakPositions.slice(0,2).join(" / ")}, then adds players ranked ahead of ADP.`}</small></div>{nextContext.map(p=><div className={`targetCard ${p.acquisition==="BUY-LOW"?"buyLow":"tradeTarget"}`} key={p.name}><div><span className={`pos ${posColor[p.pos]}`}>{p.pos}</span><em>{p.acquisition}</em></div><strong>{p.name}</strong><small>{p.tag} · Model #{p.custom} · ADP {p.adp}</small><p>{p.reason}</p></div>)}</div></section>
}

function ModelExplainer(){return <section className="modelPage"><div className="modelHero"><p className="eyebrow">FOURTH DOWN MODEL / VERSION 2026.10</p><h1>From usage<br/><em>to draft price.</em></h1><p>Fourth Down is a deterministic, position-aware value model. It does not ask only “who scores the most points?” It estimates a player&apos;s weekly range, adjusts for how those points are created, compares that output with replacement-level production, and then prices the result for your league.</p><div><b>80%</b><span>MODEL-DRIVEN BASE</span><b>20%</b><span>MARKET SANITY ANCHOR</span></div></div><div className="modelSummary"><article><span>MODEL TYPE</span><h2>Rules-based ensemble</h2><p>Transparent, hand-weighted components combine projection, role, advanced opportunity, positional value, availability and market price. It is not a neural network and does not pretend its judgments are medical facts.</p></article><article><span>PRIMARY OUTPUT</span><h2>Draft value—not raw points</h2><p>A quarterback can score more than a running back and still rank lower in one-QB because comparable quarterback production is easier to replace later.</p></article><article><span>UNCERTAINTY</span><h2>A range, not one answer</h2><p>Every player has a floor, ceiling, confidence score and upside-sensitive draft window. Wider opportunity or health uncertainty changes how aggressively the model recommends drafting.</p></article></div><div className="pipeline"><div><span>01</span><h3>MEASURE</h3><p>Collect weekly production and opportunity: carries, targets, air yards, shares, first downs, explosive plays, EPA, CPOE, WOPR and PPR points.</p></div><div><span>02</span><h3>CONTEXTUALIZE</h3><p>Add depth order, teammate competition, quarterback, team play mix, red-zone access, injury designation, age and physical profile.</p></div><div><span>03</span><h3>PROJECT</h3><p>Estimate a weekly floor, median and ceiling. Confidence reflects sample quality and role certainty, not a guarantee that the result will happen.</p></div><div><span>04</span><h3>PRICE POSITION</h3><p>Compare projected production with a startable-later replacement. League lineup rules change scarcity and therefore change value.</p></div><div><span>05</span><h3>CALIBRATE</h3><p>Apply a capped advanced-stat adjustment, injury and depth penalties, then blend 80% model price with a 20% ADP sanity anchor.</p></div></div><div className="positionModels"><div className="positionModelsHead"><p className="eyebrow">POSITION-SPECIFIC ENGINES</p><h2>Different jobs need different evidence.</h2><p>The model does not evaluate a target-earning receiver and a rushing quarterback with the same checklist.</p></div><article><span className={`pos ${posColor.QB}`}>QB</span><h3>Quarterback</h3><ul><li>Passing EPA and CPOE</li><li>Sack avoidance and TD–INT efficiency</li><li>Rushing attempts and first downs</li><li>One-QB versus Superflex replacement supply</li></ul></article><article><span className={`pos ${posColor.RB}`}>RB</span><h3>Running back</h3><ul><li>Targets per game and receiving access</li><li>Rushing first-down and explosive-run rates</li><li>EPA per touch—not carries alone</li><li>Goal-line role, depth and contingency value</li></ul></article><article><span className={`pos ${posColor.WR}`}>WR</span><h3>Wide receiver</h3><ul><li>Target share and air-yard share</li><li>WOPR opportunity concentration</li><li>First-down rate and yards after catch</li><li>Quarterback and competing target context</li></ul></article><article><span className={`pos ${posColor.TE}`}>TE</span><h3>Tight end</h3><ul><li>Routes through target and air-yard share</li><li>WOPR, first downs and YAC</li><li>Only a measured outlier gap earns scarcity credit</li><li>TE3+ is priced against a compressed streaming tier</li></ul></article></div><div className="modelEquation"><p className="eyebrow">THE RANKING STACK</p><div><b>1</b><span>WEEKLY RANGE</span><i>floor × 65% + ceiling × 35%</i></div><div><b>2</b><span>VALUE OVER REPLACEMENT</span><i>(projection − positional baseline) × lineup demand</i></div><div><b>3</b><span>ROLE + CONTEXT</span><i>premium touches + depth access + scoring environment</i></div><div><b>4</b><span>ADVANCED LAYER</span><i>position-specific efficiency and opportunity, capped at ±10 picks</i></div><div><b>5</b><span>RISK + MARKET</span><i>injury/depth drag, then 80% model + 20% ADP anchor</i></div></div><div className="modelDetails"><article><p className="eyebrow">WHAT MOVES A PLAYER UP</p><h2>Repeatable opportunity.</h2><ul><li>Targets and carries with scoring leverage</li><li>High target share plus meaningful air yards</li><li>Receiving work for running backs</li><li>Rushing contribution for quarterbacks</li><li>Clear first-team depth access</li><li>Ceiling that materially beats replacement</li></ul></article><article><p className="eyebrow">WHAT MOVES A PLAYER DOWN</p><h2>Fragile assumptions.</h2><ul><li>Backup depth without a defined role</li><li>Raw volume without first downs or efficiency</li><li>Recurring injury and uncertain return to role</li><li>Replaceable one-QB production</li><li>Small or missing samples</li><li>Market price far above model value</li></ul></article></div><div className="guardrails"><div><p className="eyebrow">MODEL GUARDRAILS</p><h2>Designed not to overreact.</h2></div><article><b>4-GAME MINIMUM</b><p>Advanced statistics do not alter rank without at least four games of verified data.</p></article><article><b>±10-PICK CAP</b><p>The advanced layer can refine a price but cannot overpower role, projection and positional value.</p></article><article><b>MISSING = NEUTRAL</b><p>A rookie or unavailable sample receives no invented historical efficiency.</p></article><article><b>NO DOUBLE-COUNTED TE SCARCITY</b><p>Replacement value, lineup demand and tier separation are applied once; only the top two projected outliers can earn an elite bonus.</p></article></div><div className="interpretGrid"><article><p className="eyebrow">HOW TO USE THE RANK</p><h2>Let the room come to you.</h2><p>Model rank is the preferred order, not an instruction to draft at that exact pick. Compare it with ADP. If the model is earlier, wait when possible and select before the value window closes.</p></article><article><p className="eyebrow">HOW TO USE THE RANGE</p><h2>Price upside intelligently.</h2><p>High-ceiling players receive wider acceptable windows. Capped-ceiling players need to fall close to—or later than—the model center before becoming attractive.</p></article><article><p className="eyebrow">HOW TO USE TEAM FIT</p><h2>Rank is not roster construction.</h2><p>The Team Analyzer applies lineup needs after player value. An elite QB or TE can solve that position, preserving bench spots for scarce RB/WR outcomes.</p></article></div><div className="limitations"><p className="eyebrow">WHAT THE MODEL CANNOT KNOW</p><h2>Useful estimates, not certainty.</h2><p>Coaching plans can change, depth charts can be misleading, touchdowns remain volatile, rookies have limited NFL samples, and public injury reports are not diagnoses. Current weights are deterministic and research-informed; they have not yet been learned by a fully trained historical machine-learning system. Use the model to make prices consistent—not to eliminate uncertainty.</p></div><div className="formulaBlock"><div><span>CONCEPTUAL FORMULA</span><code>projection + replacement edge + role + advanced signal + confidence − injury/depth risk</code></div><p>Strategy settings alter the weights and then re-sort the full pool. Safe Floor rewards stability; All or Nothing emphasizes ceiling; positional builds adjust scarcity without erasing the base model.</p></div><div className="sourceCards"><a href="https://github.com/nflverse/nflverse-data/releases/tag/stats_player" target="_blank" rel="noreferrer"><b>nflverse</b><span>Verified weekly production and opportunity fields ↗</span></a><a href="https://nflreadr.nflverse.com/articles/dictionary_player_stats.html" target="_blank" rel="noreferrer"><b>FIELD DICTIONARY</b><span>EPA, shares, WOPR, CPOE and first-down definitions ↗</span></a><a href="https://docs.sleeper.com/" target="_blank" rel="noreferrer"><b>SLEEPER</b><span>Players, depth, injuries and market trends ↗</span></a><a href="https://operations.nfl.com/gameday/technology/nfl-next-gen-stats" target="_blank" rel="noreferrer"><b>NFL NGS</b><span>Tracking and advanced-stat methodology ↗</span></a><a href="https://pubmed.ncbi.nlm.nih.gov/34878369/" target="_blank" rel="noreferrer"><b>INJURY RESEARCH</b><span>NFL hamstring recurrence evidence ↗</span></a><div><b>MODEL OUTPUT</b><span>Projections are estimates, not sourced facts or medical diagnoses.</span></div></div></section>}
function UnderdogNews({player}:{player:string}){
  const [news,setNews]=useState<NewsState>({loading:true,connected:false,posts:[],searchUrl:""});
  useEffect(()=>{let alive=true;const load=()=>{setNews(n=>({...n,loading:true}));fetch(`/api/underdog?player=${encodeURIComponent(player)}`).then(r=>r.json()).then(data=>{if(alive)setNews({...data,loading:false})}).catch(()=>{if(alive)setNews({loading:false,connected:false,posts:[],searchUrl:`https://x.com/search?q=${encodeURIComponent(`from:UnderdogNFL "${player}"`)}&f=live`,message:"News lookup unavailable."})})};load();const timer=setInterval(load,300000);return()=>{alive=false;clearInterval(timer)}},[player]);
  return <section className="underdogPanel"><div className="udHead"><div><p className="eyebrow">LIVE PLAYER WIRE</p><h2>Underdog NFL mentions</h2><span>Automatically matched to {player} · refreshes every 5 minutes</span></div><span className={news.connected?"connected":"needsKey"}>{news.connected?"● LIVE":"○ X CONNECTION NEEDED"}</span></div>{news.loading?<div className="newsEmpty">Checking @UnderdogNFL…</div>:news.posts.length?<div className="newsList">{news.posts.map(post=><a href={post.url} target="_blank" rel="noreferrer" key={post.id}><span>@UnderdogNFL · {post.created_at?new Date(post.created_at).toLocaleString():"Recent"}</span><p>{post.text}</p><b>OPEN ON X ↗</b></a>)}</div>:<div className="newsEmpty"><b>No automatic mentions loaded for {player}.</b><p>{news.message||"No matching posts were found in the recent-search window."}</p><a href={news.searchUrl} target="_blank" rel="noreferrer">SEARCH @UNDERDOGNFL LIVE ON X ↗</a></div>}<p className="newsNote">Source: @UnderdogNFL on X. Posts are shown as reported and should be verified against official team injury reports.</p></section>
}

function StealsPage({steals,pos,setPos,teams,strategy,scoring,superflex,inspect}:{steals:RankedPlayer[];pos:string;setPos:(p:string)=>void;teams:number;strategy:string;scoring:string;superflex:boolean;inspect:(p:Player)=>void}){return <section className="stealsPage"><div className="stealsHero"><div><p className="eyebrow">MODEL RANK vs ESPN ADP</p><h1>Biggest steals<br/><em>to watch.</em></h1><p>Players the Fourth Down model prices earlier than the ESPN market snapshot. A positive gap is how many picks sooner the model would draft that player.</p></div><div className="stealHeroStat"><span>LARGEST EDGE</span><b>+{steals.length?Math.round(steals[0].adp-steals[0].custom):0}</b><small>PICKS VS ESPN ADP</small></div></div><div className="stealsControls"><div className="pills">{["ALL","QB","RB","WR","TE"].map(x=><button key={x} className={pos===x?"active":""} onClick={()=>setPos(x)}>{x}</button>)}</div><span>{strategies[strategy as keyof typeof strategies].name} model · {scoring.toUpperCase()} · {superflex?"Superflex":"1QB"}</span></div><div className="stealGrid">{steals.map((p,i)=>{const edge=Math.round(p.adp-p.custom);const inj=injuryFor(p);return <article key={p.name} onClick={()=>inspect(p)}><div className="stealTop"><b>#{i+1}</b><span className={`pos ${posColor[p.pos]}`}>{p.pos}</span><em>+{edge} PICKS</em></div><h2>{p.name}</h2><p>{p.team} · {p.role}</p><div className="rankCompare"><div><span>MODEL</span><b>#{p.custom}</b><small>Round {roundFor(p.custom,teams)}</small></div><i>→</i><div><span>ESPN ADP</span><b>{p.adp}</b><small>Round {roundFor(p.adp,teams)}</small></div></div><div className="stealWhy"><span>{p.rz}/100 ROLE · {p.confidence}% CONFIDENCE · {inj.flag} RISK</span><p>{p.note}</p></div><button>OPEN PLAYER LAB →</button></article>})}</div><div className="stealsSource"><b>HOW TO READ THIS</b><p>This is a watchlist, not a command to reach. Wait for the ESPN room when possible, then draft before the market window closes. ESPN defines ADP as the average spot where a player is drafted and notes that it varies by league scoring.</p><a href="https://www.espn.com/fantasy/football/story/_/id/19541777/glossary" target="_blank" rel="noreferrer">ESPN ADP definition ↗</a></div></section>}

function HighUpsideList({players,teams,scoring,superflex,strategy,inspect}:{players:RankedPlayer[];teams:number;scoring:string;superflex:boolean;strategy:string;inspect:(p:Player)=>void}){return <section className="upsideWatch"><div className="upsideWatchHead"><div><p className="eyebrow">CEILING × OPPORTUNITY × PRICE</p><h2>High-upside players to watch</h2><p>These are not necessarily the largest ADP discounts. They have the strongest model paths to materially outperform their draft slot if the high-end outcome hits.</p></div><span>{players.length} CEILING TARGETS</span></div><div className="upsideWatchGrid">{players.map((p,i)=>{const w=upsideWindow(p,scoring,superflex,strategy),edge=Math.round(p.adp-p.custom);return <article key={p.name} onClick={()=>inspect(p)}><div><b>#{i+1}</b><span className={`pos ${posColor[p.pos]}`}>{p.pos}</span><em>{w.score} UPSIDE</em></div><h3>{p.name}</h3><small>{p.team} · {p.role}</small><div className="upsideWatchRange"><span>FLOOR <b>{p.floor.toFixed(1)}</b></span><i/><span>CEILING <b>{p.ceiling.toFixed(1)}</b></span></div><dl><div><dt>MODEL</dt><dd>#{p.custom}</dd></div><div><dt>ADP EDGE</dt><dd>{edge>=0?`+${edge}`:edge}</dd></div><div><dt>DRAFT WINDOW</dt><dd>R{roundFor(w.early,teams)}–R{roundFor(w.late,teams)}</dd></div></dl><p>{p.note}</p><button>VIEW FULL UPSIDE CASE →</button></article>})}</div><p className="upsideWatchNote"><b>MODEL NOTE:</b> Upside combines absolute ceiling, floor-to-ceiling spread, positional edge, high-value role, confidence, injury risk and current depth. A high score means a stronger path to an outlier result—not a higher probability of reaching it.</p></section>}

export function DraftLab(){
  const [tab,setTab]=useState("board"); const [query,setQuery]=useState(""); const [pos,setPos]=useState("ALL");
  const [teams,setTeams]=useState(12); const [scoring,setScoring]=useState("ppr"); const [superflex,setSuperflex]=useState(false);
  const [leagueShape,setLeagueShape]=useState<LeagueShape>("standard");const [marketSource,setMarketSource]=useState("consensus");const [marketFeeds,setMarketFeeds]=useState<MarketFeed>({});const [marketUpdated,setMarketUpdated]=useState<string|null>(null);
  const [league,setLeague]=useState<LeagueConfig>({qb:1,rb:2,wr:2,te:1,flex:1,bench:7,tePremium:0});const [showLeague,setShowLeague]=useState(false);const [nextPick,setNextPick]=useState(24);
  const [strategy,setStrategy]=useState("balanced");
  const [pool,setPool]=useState<Player[]>(players); const [depthConnected,setDepthConnected]=useState(false); const [depthUpdated,setDepthUpdated]=useState<string|null>(null);
  const [advancedStats,setAdvancedStats]=useState<Record<string,AdvancedStat>>({});
  const [selected,setSelected]=useState<Player>(players[0]); const [pick,setPick]=useState(8);
  const [boardView,setBoardView]=useState<"available"|"taken"|"queue">("available");const [taken,setTaken]=useState<string[]>([]);const [queue,setQueue]=useState<string[]>([]);const [boardReady,setBoardReady]=useState(false);
  const [mock,setMock]=useState("1. Jahmyr Gibbs\n2. Ja'Marr Chase\n3. Josh Allen\n4. Puka Nacua\n5. Derrick Henry\n6. Bijan Robinson\n7. Jalen Hurts\n8. Amon-Ra St. Brown");
  useEffect(()=>{let alive=true;fetch("/api/player-pool").then(r=>r.json()).then(data=>{if(!alive||!data.connected)return;const liveByName=new Map<string,any>(data.players.map((p:any)=>[nameKey(p.name),p]));const merged:Player[]=players.map(p=>{const live=liveByName.get(nameKey(p.name));return live?{...p,team:live.team||p.team,playerId:live.playerId,depth:live.depthOrder||null,depthLabel:live.depthPosition?String(live.depthPosition):p.pos,injuryStatus:live.injuryStatus,age:live.age,live:true}:p});const used=new Set(merged.map(p=>nameKey(p.name)));for(const raw of data.players){if(merged.length>=180)break;if(!used.has(nameKey(raw.name))){merged.push(expandedPlayer(raw,merged.length+1));used.add(nameKey(raw.name))}}setPool(merged.slice(0,180));setDepthConnected(true);setDepthUpdated(data.updatedAt);setSelected(current=>merged.find(p=>nameKey(p.name)===nameKey(current.name))||current)}).catch(()=>{});return()=>{alive=false}},[]);
  useEffect(()=>{let alive=true;fetch("/api/advanced").then(r=>r.json()).then(data=>{if(!alive||!data.connected)return;const normalized:Record<string,AdvancedStat>={};for(const [name,stat] of Object.entries(data.players||{}))normalized[nameKey(name)]=stat as AdvancedStat;setAdvancedStats(normalized)}).catch(()=>{});return()=>{alive=false}},[]);
  useEffect(()=>{let alive=true;fetch(`/api/market?teams=${teams}&scoring=${scoring}`).then(r=>r.json()).then(data=>{if(!alive||!data.connected)return;const feeds:MarketFeed={};for(const [source,rows] of Object.entries(data.markets||{})){feeds[source]={};for(const row of rows as {name:string;adp:number}[])feeds[source][nameKey(row.name)]=row.adp}setMarketFeeds(feeds);setMarketUpdated(data.updatedAt)}).catch(()=>{});return()=>{alive=false}},[teams,scoring]);
  useEffect(()=>{try{const saved=JSON.parse(localStorage.getItem("fourth-down-draft-board")||"{}");setTaken(Array.isArray(saved.taken)?saved.taken:[]);setQueue(Array.isArray(saved.queue)?saved.queue:[])}catch{}setBoardReady(true)},[]);
  useEffect(()=>{if(boardReady)localStorage.setItem("fourth-down-draft-board",JSON.stringify({taken,queue}))},[taken,queue,boardReady]);
  useEffect(()=>{if(tab!=="player"||query.trim().length<3||pool.some(p=>nameKey(p.name).includes(nameKey(query))))return;let alive=true;const timer=setTimeout(()=>{fetch(`/api/model-player?name=${encodeURIComponent(query.trim())}`).then(r=>r.json()).then(data=>{if(!alive||!data.found)return;setPool(current=>current.some(p=>nameKey(p.name)===nameKey(data.player.name))?current:[...current,data.player])}).catch(()=>{})},650);return()=>{alive=false;clearTimeout(timer)}},[query,tab,pool]);
  const ranked=useMemo(()=>pool.map(original=>{const quotes=Object.values(marketFeeds).map(feed=>feed[nameKey(original.name)]).filter((x):x is number=>Number.isFinite(x));const selectedMarket=marketSource==="consensus"?(quotes.length?quotes.reduce((a,b)=>a+b,0)/quotes.length:original.adp):(marketFeeds[marketSource]?.[nameKey(original.name)]||original.adp);const p={...original,adp:Math.round(selectedMarket*10)/10};const advanced=advancedStats[nameKey(p.name)],advancedImpact=advancedRankImpact(p,advanced),prospect=prospectImpact(p,!!advanced),supplyImpact=supplyRankImpact(p,pool,teams,superflex);return{...p,marketQuotes:quotes,advancedImpact,prospectImpact:prospect,supplyImpact,modelPrice:Math.max(1,adjustedRank(p,scoring,superflex,strategy,teams,leagueShape,league)+advancedImpact+supplyImpact+prospect)}}).sort((a,b)=>a.modelPrice-b.modelPrice||b.ceiling-a.ceiling||a.adp-b.adp).map((p,index)=>({...p,custom:index+1})),[pool,teams,scoring,superflex,strategy,advancedStats,marketFeeds,marketSource,leagueShape,league]);
  const filtered=ranked.filter(p=>{const isTaken=taken.includes(p.name),isQueued=queue.includes(p.name);return(pos==="ALL"||p.pos===pos)&&p.name.toLowerCase().includes(query.toLowerCase())&&(boardView==="available"?!isTaken:boardView==="taken"?isTaken:isQueued&&!isTaken)});
  const steals=(()=>{const candidates=ranked.filter(p=>p.adp>0&&p.adp-p.custom>=3&&(pos==="ALL"||p.pos===pos)).sort((a,b)=>(b.adp-b.custom)-(a.adp-a.custom)||b.confidence-a.confidence),chosen:typeof candidates=[],counts:Partial<Record<Pos,number>>={};for(const p of candidates){const cap=pos==="ALL"?6:24;if((counts[p.pos]||0)>=cap)continue;chosen.push(p);counts[p.pos]=(counts[p.pos]||0)+1;if(chosen.length===24)break}return chosen})();
  const highUpside=ranked.filter(p=>(pos==="ALL"||p.pos===pos)&&p.depth!==0&&(!p.depth||p.depth<=2)).sort((a,b)=>upsideWindow(b,scoring,superflex,strategy).score-upsideWindow(a,scoring,superflex,strategy).score||(b.ceiling-b.floor)-(a.ceiling-a.floor)).slice(0,12);
  const selectedRank=ranked.find(p=>nameKey(p.name)===nameKey(selected.name))?.custom||adjustedRank(selected,scoring,superflex,strategy);
  const selectedModeled=ranked.find(p=>nameKey(p.name)===nameKey(selected.name))||selected;
  const value=selected.adp-selectedRank; const targetRound=roundFor(selectedRank,teams);
  const currentDelta=pick-selectedRank; const verdict=currentDelta>=8?"SMASH DRAFT":currentDelta>=-5?"DRAFT AT COST":currentDelta>=-14?"WAIT":"DO NOT REACH"; const injury=injuryFor(selected);
  const parsed=mock.split(/\n|,/).map(x=>x.replace(/^\s*\d+[.)-]?\s*/,"").trim()).filter(Boolean);
  const mockRows=parsed.map((name,i)=>{const p=ranked.find(x=>nameKey(x.name)===nameKey(name));return {pick:i+1,p,delta:p?(i+1)-p.custom:0}});
  const known=mockRows.filter(x=>x.p); const grade=known.length?Math.max(45,Math.min(98,Math.round(82+known.reduce((s,x)=>s+x.delta,0)/known.length))):0;

  function inspect(p:Player){setSelected(p);setTab("player");window.scrollTo({top:0,behavior:"smooth"})}
  function markTaken(name:string){setTaken(current=>current.includes(name)?current:[...current,name]);setQueue(current=>current.filter(x=>x!==name))}
  function restorePlayer(name:string){setTaken(current=>current.filter(x=>x!==name))}
  function toggleQueue(name:string){if(taken.includes(name))return;setQueue(current=>current.includes(name)?current.filter(x=>x!==name):[...current,name])}
  return <main>
    <header><div className="brand"><span className="mark">4D</span><span>FOURTH DOWN <small>MODEL LAB</small></span></div><nav>{[["board","Model Board"],["steals","Biggest Steals"],["player","Player Lab"],["team","Team Analyzer"],["validation","Validation"],["model","The Model"]].map(([id,label])=><button key={id} className={tab===id?"active":""} onClick={()=>setTab(id)}>{label}</button>)}</nav><div className="status"><i/> MODEL ONLINE</div></header>
    <section className="hero"><div><p className="eyebrow">FOURTH DOWN MODEL / 2026 DRAFT INTELLIGENCE</p><div className="modelStamp">80% MODEL-DRIVEN · 20% MARKET ANCHOR</div><h1>Model the role.<br/><em>Price the edge.</em></h1><p className="lede">Rankings start with projected opportunity, ceiling, availability and positional value over replacement. ADP is a secondary sanity check—not the answer.</p></div><div className="heroStats"><div><b>{pool.length}</b><span>MODELED PLAYERS</span></div><div><b>VORP</b><span>POSITIONAL VALUE ENGINE</span></div><div><b>{depthConnected?"LIVE":"SYNC"}</b><span>SITUATION FEED</span></div></div></section>
    <section className="controlbar"><label>LEAGUE<select value={teams} onChange={e=>setTeams(+e.target.value)}><option>10</option><option>12</option><option>14</option></select></label><label>SCORING<select value={scoring} onChange={e=>setScoring(e.target.value)}><option value="ppr">Full PPR</option><option value="half">Half PPR</option><option value="standard">Standard</option></select></label><label>MARKET<select value={marketSource} onChange={e=>setMarketSource(e.target.value)}><option value="consensus">Live consensus</option><option value="ffc">FFC ADP</option><option value="fantasycalc">FantasyCalc</option></select></label><label>STRATEGY<select value={strategy} onChange={e=>setStrategy(e.target.value)}>{Object.entries(strategies).map(([id,s])=><option key={id} value={id}>{s.name}</option>)}</select></label><label className="switch">SUPERFLEX<button aria-label="Toggle superflex" className={superflex?"on":""} onClick={()=>setSuperflex(!superflex)}><span/></button></label><button className="advancedLeagueButton" onClick={()=>setShowLeague(x=>!x)}>{showLeague?"CLOSE":"CUSTOM LEAGUE"}</button><span className="updated">{marketUpdated?`LIVE MARKET · ${new Date(marketUpdated).toLocaleDateString()}`:"MARKET SYNCING"}</span></section>
    {showLeague&&<section className="leagueBuilder"><div><p className="eyebrow">EXACT LEAGUE SETTINGS</p><h2>Replacement value follows your room.</h2><p>Starter demand and bench depth now move each position&apos;s replacement baseline.</p></div><div className="leagueInputs">{([['qb','QB'],['rb','RB'],['wr','WR'],['te','TE'],['flex','FLEX'],['bench','BENCH']] as const).map(([key,label])=><label key={key}>{label}<input type="number" min={key==='bench'?2:0} max={key==='bench'?15:4} value={league[key]} onChange={e=>setLeague({...league,[key]:Math.max(0,+e.target.value)})}/></label>)}<label>TE PREMIUM<select value={league.tePremium} onChange={e=>setLeague({...league,tePremium:+e.target.value})}><option value="0">None</option><option value="0.5">+0.5 PPR</option><option value="1">+1.0 PPR</option></select></label></div></section>}

    {tab==="board"&&<section className="panel board"><div className="strategyStrip">{Object.entries(strategies).map(([id,s])=><button key={id} className={strategy===id?"chosen":""} onClick={()=>setStrategy(id)}><span>{s.tag}</span><b>{s.name}</b><small>{s.desc}</small></button>)}</div><div className="draftBoardTabs"><div><p className="eyebrow">MY DRAFT BOARD</p><h2>Run the room.</h2></div><div>{([['available',`AVAILABLE ${ranked.length-taken.length}`],['taken',`TAKEN ${taken.length}`],['queue',`QUEUE ${queue.filter(x=>!taken.includes(x)).length}`]] as const).map(([id,label])=><button key={id} className={boardView===id?"active":""} onClick={()=>setBoardView(id)}>{label}</button>)}</div><button className="resetBoard" disabled={!taken.length&&!queue.length} onClick={()=>{if(window.confirm("Clear every taken player and queue entry?")){setTaken([]);setQueue([])}}}>RESET BOARD</button></div><div className="survivalControl"><div><p className="eyebrow">DRAFT SURVIVAL ENGINE</p><h3>Will they make it back?</h3><span>Probability uses live ADP, market dispersion, confidence and outcome volatility.</span></div><label>YOUR NEXT PICK<input type="number" min="2" max="180" value={nextPick} onChange={e=>setNextPick(Math.max(2,+e.target.value))}/></label></div><div className="sectionHead"><div><p className="eyebrow">MODEL × MARKET × AVAILABILITY</p><h2>{boardView==="available"?strategies[strategy as keyof typeof strategies].name:boardView==="taken"?"Drafted players":"Your priority queue"}</h2></div><div className="filters"><input aria-label="Search players" placeholder="Search player…" value={query} onChange={e=>setQuery(e.target.value)}/><div className="pills">{["ALL","QB","RB","WR","TE"].map(x=><button key={x} className={pos===x?"active":""} onClick={()=>setPos(x)}>{x}</button>)}</div></div></div><div className="tableWrap"><table><thead><tr><th>RK</th><th>PLAYER</th><th>MODEL RANGE</th><th>ROLE QUALITY</th><th>INJURY LENS</th><th>MARKET</th><th>AT PICK {nextPick}</th><th>STATUS</th><th>DRAFT CONTROLS</th></tr></thead><tbody>{filtered.map((p)=>{const val=p.adp-p.custom,inj=injuryFor(p),isQueued=queue.includes(p.name),survival=survivalProbability(p,nextPick);return <tr className={taken.includes(p.name)?"takenRow":isQueued?"queuedRow":""} key={p.name} onClick={()=>inspect(p)}><td className="rank">{p.custom}</td><td><div className="player"><span className={`pos ${posColor[p.pos]}`}>{p.pos}</span><div><b>{p.name}</b><small>{p.team} · {p.role}</small></div></div></td><td><b>{p.floor.toFixed(1)}–{p.ceiling.toFixed(1)}</b><small>PPR / WK</small></td><td><div className="meter"><span style={{width:`${p.rz}%`}}/></div><small>{p.rz} / 100</small></td><td><span className={`risk risk${inj.flag}`}>{inj.flag}</span><small>{inj.risk}/100 index</small></td><td><b>ADP {p.adp}</b><small>Round {roundFor(p.adp,teams)}</small></td><td><b className={survival<30?"survivalLow":survival>65?"survivalHigh":""}>{survival}%</b><small>{survival<30?"DRAFT NOW":survival>65?"LIKELY TO LAST":"COIN FLIP"}</small></td><td><span className={`call ${taken.includes(p.name)?"fade":isQueued?"value":val>=5?"value":val<=-6?"fade":"fair"}`}>{taken.includes(p.name)?"TAKEN":isQueued?"QUEUED":val>=5?`VALUE +${Math.round(val)}`:val<=-6?"PASS AT ADP":"AVAILABLE"}</span></td><td><div className="draftActions" onClick={e=>e.stopPropagation()}>{taken.includes(p.name)?<button onClick={()=>restorePlayer(p.name)}>RESTORE</button>:<><button className={isQueued?"queued":""} onClick={()=>toggleQueue(p.name)}>{isQueued?"− QUEUE":"+ QUEUE"}</button><button className="takePlayer" onClick={()=>markTaken(p.name)}>MARK TAKEN</button></>}</div></td></tr>})}</tbody></table>{!filtered.length&&<div className="emptyBoard"><b>{boardView==="queue"?"Your queue is empty.":boardView==="taken"?"No players have been marked taken.":"No matching players remain available."}</b><span>{boardView==="queue"?"Add players from the Available tab.":"Change a filter or restore a player."}</span></div>}</div></section>}

    {tab==="player"&&<section className="playerLab"><div className="playerPicker"><p className="eyebrow">PLAYER LAB</p><input aria-label="Find a player" placeholder="Type a player name…" value={query} onChange={e=>setQuery(e.target.value)}/><div>{ranked.filter(p=>p.name.toLowerCase().includes(query.toLowerCase())).slice(0,8).map(p=><button className={selected.name===p.name?"selected":""} onClick={()=>setSelected(p)} key={p.name}><span className={`pos ${posColor[p.pos]}`}>{p.pos}</span>{p.name}<small>#{p.custom}</small></button>)}</div></div><div className="analysisCard"><div className="playerTitle"><span className={`pos big ${posColor[selected.pos]}`}>{selected.pos}</span><div><p>{selected.team} · {selected.role}</p><h2>{selected.name}</h2></div><span className="confidence">{selected.confidence}%<small>CONFIDENCE</small></span></div><div className="verdict"><div><p>{strategies[strategy as keyof typeof strategies].name.toUpperCase()} · THE CALL AT PICK {pick}</p><strong>{verdict}</strong><span>{currentDelta>=0?`${currentDelta} picks of model value`:`You are reaching ${Math.abs(currentDelta)} picks`}</span></div><div className="pickControl"><label>YOUR CURRENT PICK</label><input type="range" min="1" max="120" value={pick} onChange={e=>setPick(+e.target.value)}/><b>{pick}</b></div></div><div className="metrics"><div><span>STRATEGY RANK</span><b>#{adjustedRank(selected,scoring,superflex,strategy)}</b></div><div><span>MARKET ADP</span><b>{selected.adp}</b></div><div><span>BEST ROUND</span><b>{targetRound}{targetRound===1?"st":targetRound===2?"nd":targetRound===3?"rd":"th"}</b></div><div><span>HIGH-VALUE ROLE</span><b>{selected.rz}</b></div></div><div className="range"><div className="rangeLabels"><span>FLOOR {selected.floor}</span><b>WEEKLY OUTCOME RANGE</b><span>CEILING {selected.ceiling}</span></div><div><i style={{left:`${selected.floor*2.8}%`,right:`${100-selected.ceiling*2.8}%`}}/><span style={{left:`${((selected.floor+selected.ceiling)/2)*2.8}%`}}/></div></div><div className="injuryPanel"><div className="riskDial" style={{background:`conic-gradient(var(--acid) 0 ${100-injury.risk}%, #ff6648 ${100-injury.risk}% 100%)`}}><span>{injury.risk}<small>RISK INDEX</small></span></div><div><p className="eyebrow">AVAILABILITY LENS · {injury.flag}</p><h3>{injury.prior}</h3><div className="injuryFacts"><span><b>{injury.missed}</b> recent games missed</span><span><b>↗</b> {injury.trend}</span></div><p>Risk blends recent missed time, recurrence patterns, workload change and experience. It is an availability signal—not a medical diagnosis or claim that injury is inevitable.</p></div></div><div className="scout"><p className="eyebrow">MODEL READ</p><h3>{selected.note}</h3><p>{selected.rz>=90?"Elite":"Useful"} scoring-zone involvement supports the ceiling. {value>=5?"The market is leaving a measurable discount.":value<=-5?"The player is expensive relative to the model; let someone else pay full freight.":"Price and model value are closely aligned."}</p></div><div className="evidence"><b>EVIDENCE LENS</b><span>Raw carry totals are not treated as automatic injury risk. The model emphasizes prior absence, recurrence and abrupt workload change.</span><a href="https://pmc.ncbi.nlm.nih.gov/articles/PMC5347429/" target="_blank" rel="noreferrer">RB carries study ↗</a><a href="https://pubmed.ncbi.nlm.nih.gov/34878369/" target="_blank" rel="noreferrer">NFL hamstring recurrence ↗</a></div></div></section>}

    {tab==="steals"&&<StealsPage steals={steals} pos={pos} setPos={setPos} teams={teams} strategy={strategy} scoring={scoring} superflex={superflex} inspect={inspect}/>}    
    {tab==="steals"&&<HighUpsideList players={highUpside} teams={teams} scoring={scoring} superflex={superflex} strategy={strategy} inspect={inspect}/>}    
    {tab==="player"&&<UpsideDraftWindow player={selected} scoring={scoring} superflex={superflex} strategy={strategy} teams={teams} pick={pick}/>}    
    {tab==="player"&&<PositionalValuePanel player={selected} superflex={superflex}/>}    
    {tab==="player"&&<ModelFactorBreakdown player={selectedModeled} superflex={superflex} teams={teams} shape={leagueShape} league={league} advanced={advancedStats[nameKey(selected.name)]}/>}
    {tab==="player"&&<OnDemandModelContext player={selected}/>}    
    {tab==="player"&&<TeamAndInjuryContext player={selected}/>}    
    {tab==="player"&&<AdvancedAnalysis player={selected}/>}    
    {tab==="player"&&<FreePlayerWire player={selected}/>}    
    {tab==="player"&&<PastPlayerUpdates player={selected}/>}    
    {tab==="team"&&<TeamAnalyzer ranked={ranked}/>}    
    {tab==="validation"&&<ValidationPage ranked={ranked}/>}
    {tab==="model"&&<ModelExplainer/>}    
    {tab==="mock"&&<section className="mockGrid"><div className="mockInput"><p className="eyebrow">MOCK DRAFT ANALYZER</p><h2>Paste the room.<br/>Find the mistakes.</h2><p>One pick per line. Numbering is optional. We’ll compare every recognized player with your custom board.</p><textarea aria-label="Mock draft picks" value={mock} onChange={e=>setMock(e.target.value)}/><div className="hint">Try: “1. Jahmyr Gibbs” or simply “Puka Nacua”</div></div><div className="gradeCard"><div className="gradeTop"><div><span>ROOM EFFICIENCY</span><strong>{grade||"—"}</strong><small>/ 100</small></div><p>{known.length} of {parsed.length} picks recognized · {strategies[strategy as keyof typeof strategies].name}</p></div><div className="draftFeed">{mockRows.map((x,i)=><div key={i}><b>{i+1}</b>{x.p?<><span className={`pos ${posColor[x.p.pos]}`}>{x.p.pos}</span><div><strong>{x.p.name}</strong><small>Strategy #{adjustedRank(x.p,scoring,superflex,strategy)} · ADP {x.p.adp}</small></div><em className={x.delta>=6?"steal":x.delta<=-6?"reach":"fair"}>{x.delta>=6?`STEAL +${x.delta}`:x.delta<=-6?`REACH ${x.delta}`:"FAIR"}</em></>:<><div><strong>{x.p?x.p.name:parsed[i]}</strong><small>Not in current board</small></div></>}</div>)}</div>{known.length>0&&<div className="takeaway"><span>ROOM SIGNAL · {strategies[strategy as keyof typeof strategies].name.toUpperCase()}</span><p>{known.some(x=>x.delta<=-10)?"The room is paying for names early. Stay patient and attack the value pocket one tier later.":"This room is drafting close to market. Win with roster construction and positional runs, not aggressive reaches."}</p></div>}</div></section>}
    {(tab==="board"||tab==="player")&&<section className="rosterRail"><div><span>YOUR QUEUE · {queue.filter(x=>!taken.includes(x)).length}</span><p>{queue.filter(x=>!taken.includes(x)).length?queue.filter(x=>!taken.includes(x)).slice(0,6).join(" · "):"Add priority players from the board or Player Lab."}{queue.filter(x=>!taken.includes(x)).length>6?` · +${queue.filter(x=>!taken.includes(x)).length-6} more`:""}</p></div><div className="railActions"><button onClick={()=>{setTab("board");setBoardView("queue");window.scrollTo({top:380,behavior:"smooth"})}}>OPEN QUEUE</button><button className={queue.includes(selected.name)?"removeQueue":""} disabled={taken.includes(selected.name)} onClick={()=>toggleQueue(selected.name)}>{queue.includes(selected.name)?"− REMOVE":"+ ADD"} {selected.name.split(" ").pop()?.toUpperCase()}</button></div></section>}
    <footer><div className="brand"><span className="mark">4D</span><span>FOURTH DOWN</span></div><p>Model guidance, not certainty. Rankings blend historical role signals with an Aug. 18 market anchor.</p><span>2026 · FULL PPR</span></footer>
  </main>
}
